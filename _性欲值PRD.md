# 性欲值重写 · 设计记录

状态：**设计定稿，代码已落地。** 2026-08-31 前的「提案」已由
`trellis/TASK-05_性欲值待拍板.md` 的 13 条拍板（2026-09-03）收口并全部实现。
这份文档降级为设计存档——历史提案与推演过程，查「为什么」看它，
查「现在算什么」看 `_性欲值_现行口径.md`。
最后更新：2026-09-03（TASK-05 落地收口；此前 2026-08-31 第五轮摆数）
上一轮：2026-08-30（第四轮，重写 §四 产程三段窗口：底层照通用书、放大层照过载书，
中段改「顶满了到不了」、娩出段撤掉「不经过唤起」那条锁，两个结构缺口都消掉）
参考来源新增：`06_体感/下体体感.md`（§10 BH 触发表、§11 高潮孕期体感与前驱联动、
§11b 两档高潮真源、§12 连续与钝麻）、`06_体感/孕肚体感.md`（§6 胎动与宫缩触发七类表）、
`02_身体与时间/生理机制_子宫收缩.md`（BH／前驱／真宫缩唯一真源）、
`02_身体与时间/生理机制_催产素联动.md`。**这四份都是角色向文档，只取机制、不取次数。**
参考来源：`世界书/墨墨露出1125.json`（#88/#89/#90/#91/#167）、
`世界书/孕期身体_玩法与过载/`（10 总则、11 反应字典、22 到不了与停不下）、
本插件既有的尿意两线设计与宫内压基线设计。

**这个值是什么：尿意是存量，性欲是状态。** 尿意答「里面攒了多少」，
性欲答「她现在被撩到什么程度」。这一句解释了后面全部结构——为什么要基线、
为什么自己会衰减、为什么顶到天花板也不强制出事、为什么释放不归零、
为什么可以「顶着但到不了」。墨墨那套是**高潮进度条**（它自己这么说的），
所以它的 100 必须等于高潮，才需要外挂一条禁止规则；我们这个是状态，
高潮是另一件独立的事件。

---

## 一、已定的结构

1. **重写，不共存。** 替换现有 `base.libido`，不新开第二个值。
2. **搬进 `profile.metabolism`。** 因为 `addMetabolismValue` 读写的是
   `profile.metabolism[key]`，要用它那套 `source` 分档权重就必须是代谢键。
   加进 `METABOLISM_KEYS` 一处，序列化／每 tick 夹边界／补丁键集／标签都自动跟上。
3. **两条线，三个点**（照尿意的形状）：
   - 满线 `urge` —— 她自己知道想要了，开始要压着。**越过不等于会出事。**
   - 事件门槛 —— 落在 `urge + (hard-urge)*0.5`。
   - 天花板 `hard` —— 值不再往上。
   **不掷骰。** 高潮的条件是「在爆档连续 N 分钟、且期间刺激没断」，不问是哪一类
   —— 弱的类别本来就停在门槛以下、进不了爆档，筛选已经由停留点做完了。
   原先那条「期间必须有直接类刺激」的门是多余的，去掉。
4. **基线必须有，值落回基线而不落回 0。**
   依据是过载书 22 条：「停手只是把临界拉回到高位底噪……她没有真正退下来过，
   只是在最高位和临界之间小幅震荡。**这一条是本条全部张力的来源。**」
5. **档位读「高出基线多少」，不读绝对值。** 同宫内压。否则孕期基线一抬就永久卡高档。
6. **三个旋钮，但分工不是三等分：**
   | 旋钮 | 管什么 | 副作用 |
   |---|---|---|
   | 基线 | 起点在哪（→ 欲求不满） | 无，档位读差值 |
   | 倍率 | 爬得多快（敏感度） | 无，刻度不动 |
   | 线 | 多少算满（容量／结构） | 会连带改刻度 |
   - **生理期只动倍率。** 激素变的是敏感度，不是解剖结构：起点没变、容量没变，
     变的只是同样的触碰感觉更强。所以基线不该动（排卵期不会「什么都不做也一直悬着」），
     线也不该动。
   - **孕期主要动基线和线，不靠全局倍率。** 见下面第 7、8 条。
   - 既有代码就是这么分的：周期效应全是速率（`applyCycleBreastNeedGain`
     黄体期 0.15/h、月经期 0.10/h；排卵期 `delta * 0.05`），而 `URINE_LINES`
     只列孕期与产程，周期阶段根本不在表上。
7. **孕期的「敏感」＝桶变浅，不是水龙头变大。** 基线抬 + 满线降，
   中间可走的距离从约 90 压到约 25，同一份刺激效果约四倍，没乘任何倍率。
   对照过载书 22 条：它写的是「从开始到临界的**距离**比常人短得多」，
   不是「加得快」。而且只有「距离短」这种做法才同时兑现另两句
   ——「停手不会退回零」「她没有真正退下来过」，倍率做不到那两句。
   按重要性排：
   1. **基线往上** —— 一个改动带出三件事：起点高、停手落在高位退不回去、
      什么都不做也一直悬着（欲求不满）。倍率一样都做不到。
   2. **满线往下** —— 「距离比常人短得多」的另一半。
   3. **只放大轻的那几类** —— 见第 8 条。
   基线不是 0，所以「完全没人碰她」这件事非孕期是干净的、孕期是一直悬着的,
   只靠基线一个数就做出来了。
8. **孕期的偏向抬升只给环境与轻档，不给直接类。** 孕期的敏感不是全面变敏感，
   是原本不算刺激的东西变成了刺激——坐着顶着、走路磨着、衣服箍着。
   直接上手那一类本来就管用，再乘一遍就过头了。
   这一条顺带把 22 条那句「走得越多越难受」实现掉，不用另写机制。
9. **孕期要不要再加一个全局倍率：可选，不是主力。** 前两条已经给了约四倍，
   要让孕期在「到得快」这一项上也超过排卵期就加，量级 1.2 那种，不要 2。
   （产后哺乳期周期回来，那时候两个旋钮会同时在。）
10. **孕期不是一路往上，前面有一段下凹。** 曲线形状（用户口径）：
    刚受孕时**不变**（基线还没涨、系数还是 1，所以看不出变化）→ 早孕反应出来后
    开始降 → 反应渐强、一路降到底 → 反应渐退、**还没完全消失时就已回到孕前水平**
    → 之后转为上升，接第 7 条那套桶变浅。
    最后那句不用另写机制，是两条线交叉出来的：系数爬回 1 的时候基线已经悄悄涨了一截。
11. **孕早期要动的是基线，不只是系数。** 「想不想要」就是这个值本身
    ——低档管的正是「她打算做什么」。只压系数的话她还坐在一个不低的静息值上
    （看起来就是想要），只是碰她没反应，那是孕晚期那张脸，不是孕早期。

    | | 基线 | 系数 | 出来的样子 |
    |---|---|---|---|
    | 孕早期 | **往下**，压到孕前以下 | 往下 | 自己不起念，被弄也起不来 → 不做 |
    | 孕晚期 | 往上（第 7 条） | **保持 1** | 很想要，值高、离线近 → 做 |

    两个旋钮在孕早期是分工的：基线往下管「她自己不想」，系数往下管「弄她也不太顶用」。
    少了后者，重刺激照样能把她从低基线抬到跟平时一样的高度，就变成「一碰还是能到」，
    跟「孕早期真的不会做」对不上。
12. **孕晚期不给系数。** 「撑不住」不是性欲这个值的活——那是体力、姿势、重量，
    归行动能力那一摊。「想要但撑不住还是做了」是高性欲 × 低行动能力两个值叠出来的，
    不该在性欲里预先扣一刀。
13. **下凹的形状做成三个旋钮，放 `bio`，注册时写一次不再动。**

    | 旋钮 | 管什么 | 差异 |
    |---|---|---|
    | 起点 | 第几周开始往下走 | 4～8 周，多数 6～7；4 周是下限、不是矛盾 |
    | 深度 | 最低压到多少 | 从几乎没感觉到完全没兴致；填 0 则整条下凹消失，曲线退回单调上升 |
    | 回位 | 第几周回到 1 | 有人 10 周，有人拖到 16 周 |

    最低点不单独给，放在起点到回位之间靠前——生理上先掉得快、后爬得慢，少一个要填的数。

    **填法照 `fetalPushback` 那个形状**（[tools.js:156](scripts/tools.js#L156)
    「轻 5／中 10／强 20」，只给锚点不给表，允许范围留到 30）：
    设定里写了「七周开始」就填 7；没写则模型在 5～7 之间自己挑；
    范围外的（4 周、或拖到 20 周）只有用户明确写了才填得进去。
    这跟「级给区间不给固定值」是同一个形状，不是新做法。

    这三个数只在注册时写一次、之后不动，所以不存在「重开一次填的不一样」，
    也不违反「不要每轮让模型维护症状状态」那条——`bio` 那一排跟
    `birthDifficulty`、`recoveryDays` 同货架，建卡设一次就完。
    **不需要早孕反应字段**（用户口径：有这个说法就够了，通用事件书里也有）。

    **种族层那条撤掉：** `bio.breedTolerance` 虽然现成
    （[tools.js:1241](scripts/tools.js#L1241) 注释「这具身体多能扛妊娠」，人类 1、
    有的种族 0.01 有的 5），但它是从种族算出来的
    （[tools.js:1245](scripts/tools.js#L1245)），只玩人类的话全是 1，
    当旋钮等于没旋钮。所以必须是个体字段。

---

## 二、数值

### 现成的锚点（这些是代码里已经拍板过的数，摆位时照它们反推，不要凭空定）

**方法论先说**：尿意那套的定法不是「觉得 52 差不多」，是**先定这件事该花多久、再反推线**
——「入盆后约 40/小时，余量只留 10 正好是一刻钟」（[metabolism_config.js:58](scripts/metabolism_config.js#L58)）。
性欲和乳意的每一个数都该这么来。

| 现成的数 | 值 | 借它什么 |
|---|---|---|
| 刻度总量 | `BASE_METABOLISM_CAP = 150`（~~扩容 200~~ 扩容已随三签删除，只剩 150） | 所有值共用这把尺 |
| 尿意两条线 | 非孕 100/150 → 孕早 92/132 → 孕中 80/108 → 孕晚起 64/84 → **入盆 52/62** → 产后 75/105 | 孕期怎么压线的整条形状；入盆是唯一断点 |
| 档位切法 | 满＝urge；爆＝urge+(hard−urge)×0.5；高/中/低＝urge 的 .75/.5/.25 | 直接复用，性欲不另立 |
| 分档权重 | 无 1/0.5、低 1/1、中 1/1、高 0.5/1.5、满 0/2、爆 0/2；高位 decayPerHour 3~5 | 性欲要**自己一张表**（方向相反），但档数与量级照它 |
| 其余需求速率 | 3~5/**小时** | 性欲快约百倍＝「唤起不会留着」的量级来源 |
| 区间形状 | `fetalPushback` 轻 5／中 10／强 20，上限留到 30 | 三级区间给锚点不给表；上限留头 |
| 分娩痛 | 按 phase 给区间（着冠 8~9）＋叠个体修正，见 [tools.js:3598](scripts/tools.js#L3598) | **按阶段给区间、区间内插值、再叠个体修正**——这个形状性欲和乳意都该照抄 |
| 「被顶住下不来」 | 前驱地板 26（≈17%）；真前驱 26→58 | 乳意自发溢出地板的量级起点 |
| 「两次分明的事件」 | 前驱冷却 8 小时 | 不应期问同一个问题，但量级差两个数量级（分钟级） |
| 阶段天数 | 孕早 84／孕中 105／孕晚 63／临产 28 | 下凹曲线的横轴；**注意回位可能落在孕早期之外** |

**前驱那一整套本身不改、也碰不到。** 三样（宫缩／胎动／前驱）已定成不扣值、只进正文，
所以性欲乳意两次重写没有一处读写 `prodromal_config.js`。列在这里只当样板。

**时间粒度分三层，不统一**：前驱用 `passedHours`（小时尺度的事，够用）；
**性欲必须用 `deltaMinutes`**（见下一节）；乳意用小时，但排乳反射是几十秒的事，
所以它只能是事件、不能是速率。

### 时间粒度（这条是硬约束，不是提案）

**衰减必须用 `tick.deltaMinutes`，不能用 `passedHours`。**
`buildTimeTick`（tools.js:4485）里 `passedHours: Math.floor(totalHourMinutes / 60)`
是整数小时配进位器。性爱场景一轮推 5-15 分钟，多数轮 `passedHours = 0`，
偶尔跳 1 —— 按小时衰减的表现不是滑落，是「连着几轮不动，然后一次掉一大块」。
`deltaMinutes` / `deltaDays` 在同一个 tick 对象里，不量化，改读它是纯新增。

### 衰减（提案：比例已定，具体数未定）

**每分钟掉「超出基线」部分的一个固定比例。落到基线为止。**
**衰减对每一分钟都跑，包括正在被弄的那些分钟。** 这是「顶着但到不了」的来路：
连续刺激不是直线上升，是越走越平，最后停住。若只在停手时衰减，
连续刺激必然撞天花板，那就退回墨墨那个进度条了。

用百分比而不是固定值的理由：一个参数同时产出两种表现——非孕期基线低，
超出多，掉得多，读作「明显回落」；孕晚期基线高，超出少，掉得少，读作「退潮幅度很小」。

**衰减不按刺激级别定。** 漏速只看当前值有多高，跟正在用哪一级的刺激无关。
（不是「轻档配小衰减、重档配大衰减」那种一一对应的做法——那种做法下
每一档都恒定净涨，永远不会有停留点。）

**停留点公式（关键）：** 一直给固定速率，值会停在
`基线 + 每分钟量 × K`，K 由衰减比例决定（20% 时 K 在 4~5 之间，
差别只是「先加还是先减」，那个顺序还没定）。
所以**每分钟量、衰减比例、满线三个数不能各定各的，必须一起摆**。

### 硬约束：高切轻不许下降（摆数值时当条件用）

因为漏速只看高度，所以「从重的换成轻的」天然会让值往下走——它在往轻档
自己那个停留点落。用户口径：**这个不许发生。只要人还在碰她，值就得还在涨；
唯一允许下降的窗口是不应期。**

满足它不用改机制，只要一条算术约束：

> **身体接触最轻那一档的每分钟量 > 天花板处的每分钟漏量**

满足了，从多重切下来都是净涨；跌只发生在**完全停手**的时候。

**代价（必须一起记住）：** 这么定之后**身体接触类就没有停留点了**——
只要一直碰，值会一路涨到天花板。所以「顶着但到不了」不再靠停留点实现，
改由时间实现：到顶之后要在爆档待够 N 分钟才算事件。

**弱的类别靠什么筛：** 不是靠「类别」，是靠**每分钟量小于高处的漏速**。
想象类、环境类的每分钟量压不过漏速，所以它们仍然有停留点、仍然到不了
（「光靠幻想／光靠磨永远到不了」那条还在）。**分界线就是漏速本身**，
约束一加上去，身体接触类自动全部越线、弱的自动全部不越线。
（我早先说「弱的靠类别筛」，那句不准，以这条为准。）

### 不应期：做成乘在所有刺激上的系数，从很小回到 1

不是完全不给刺激。理由是它跟「碰不得、一碰就躲」本来就是同一件事——
不是碰她没反应，是碰她给的东西没了。

这也是**唯一允许值下降的窗口**：系数很小的时候，加的压不过漏的，值往下走。
正好是不应期该有的样子。（三档高潮里只有中／大留不应期，小的不留，见 §三。）

**现在 PRD 里的旧数是不自洽的**（旧的直接类 15/分 配 20% 衰减停在基线上方 60~75，
连满线 100 都过不去，意味着非孕期怎么弄都到不了满档）。所以要么衰减小于 20%，
要么身体接触类的上界大于 15。20% 还有另一个疑点：停手约 25 分钟就回基线了，
跟 22 条「只在最高位和临界之间小幅震荡」比，黏性可能不够。
**上面那条硬约束还会再压一次 20%**：天花板处漏得越狠，最轻那一档就得越大，
两头会顶起来。所以衰减比例大概要往小走。

**白拿的好处：** 因为衰减快，隔了半小时以上的两轮，先算加还是先算减结果一样
（停手 5 分钟剩 33%、10 分钟剩 11%、25 分钟剩 1%）。所以模型不用报
「这事发生在这一轮的开头还是结尾」——那件事它本来也报不准。

对照：其余需求是 3-5/**小时**。性欲快约百倍，这就是「唤起不会留着」。

### 刺激怎么报给系统（这一整块替换旧的四类固定值表）

**按分钟计，不按次计。** 过载书 10 条：「档位主要靠**叠加和时长**往上走，
不靠加狠。同一件事做得更久、几样同时来，档位自己就上去了。」

**两根轴，各管一件事：**
- **类别** 管这一份要不要被特殊对待（低位给想象加成、孕期只放大环境、
  几样同时来分别相加）。
- **级** 管每分钟加多少。

**只有一根轴报级。** 模型每轮报「哪一类 + 几分钟」，只有身体接触那一类再多报一个级：

| 类别 | 报什么 | 为什么 |
|---|---|---|
| 想象类 | 只报分钟 | 强度固定，没有轻重之分 |
| 环境类 | 只报分钟 | 强度固定——走一天路，裤缝磨的力度就那样 |
| 身体接触类 | 分钟 + 级 | 有人在动手，可以隔着也可以直接、可以慢也可以快 |

**不做九宫格**（心理轻中重／环境轻中重／…）。不是因为格子多，是因为大半是空的：
「心理·重」和「心理·轻」在身体上没有区别，「环境·重」不存在。

**级只在身体接触类内部用：**

| 级 | 判据 |
|---|---|
| 轻 | 隔着外层衣物；或直接碰但不是核心部位 |
| 中 | 隔一层薄的碰核心；或直接碰核心但轻、慢 |
| 重 | 直接碰核心，且用力或快 |

- **不按器具分级。** 同一个道具用法不同落在不同级。手、口、道具、插入都在同一根轴上。
- **隔衣不单独立类。** 它已经是轻／中的分界线；再立一类等于同一件事报两遍。
  而且它在「要不要特殊对待」这三件事上跟直接接触同进同退，当不了类别。
  过载书自己也是这个用法：「往上靠时长和不放手，**往下是隔着衣服**」——
  它把隔衣当同一件事的减档方向。
- **第二根轴是「多用力、多快」，不是「不间断／躲不开」。** 持续多久已经由分钟数管了，
  再按那个降一档等于时长扣两遍。
- **「躲不开」（被按住、绑着）不单独给东西**：它影响的是她能不能让刺激停，
  会自然体现在分钟数里。

**级给区间，不给固定值。** 模型在区间里自己取一个数：级决定区间，使多大劲决定
在区间里取哪个。所以「隔着睡衣蹭乳头」取轻档区间的顶、「摸后颈」取底，
**不需要加第四档**。区间本来就是干这个的。

插件里已有同形状的先例：`fetalPushback` 是 `minimum: 0, maximum: 30`
（tools.js:172）配「轻 5／中 10／强 20」的例子（tools.js:156）。
注意它的上限比最重的例子还高 —— 留头给例子里没有的极端情况。

**区间上界不能随便定**：停留点 = 基线 + 每分钟量 × K，所以上界只要超过
`(天花板 − 基线) ÷ K`，再往上就没区别了，都是钉在天花板上。范围定太宽，
宽出来那段刻度是废的，而且模型有往中间挤的倾向。例子要压在两头和中间，
不能全挤在中间。

**分工：** 代码定分类、区间、天花板、衰减、档位、正文口径；
模型只定「哪一类、多少分钟、区间里取几」。所以它就算一路报高，
值也只会钉在天花板上，跑不飞。

**刺激分钟 ≠ 本轮分钟。** 一轮可能覆盖半小时但只有五分钟在动手，
所以要单独报，代码再用 `deltaMinutes` 掐住，报多了截掉。
估分钟这件事模型每轮已经在做（`bsPassedTime` 就要它先算本轮过了多久，
tracker_prompt_context.js:282），不是新负担。

**墨墨那条反刷分规则必须删掉**（#91「相同方式出现多次仅进行单次计算」）。
它存在是因为墨墨没有衰减，不设限会被刷满。有了衰减那条不但不需要、而且必须去，
否则「靠时长往上走」实现不了。衰减和反刷分是同一件工作的两种做法，衰减那种更好。

### 刺激清单的素材来源（grok，2026-08-30）

**只当例子来源用，它自己的分级判据不能用。** 它按「有没有明显湿润／搏动」判级，
那是数值的**结果**——要按结果判进来的刺激算几级，模型得先知道她现在多兴奋，绕回去了。
它重级里的环境类几乎全是「之前被弄过、内裤湿了贴着走路」，那不是更重的刺激，
是同一种刺激在高位时更管用 = 我们的分档权重，进清单会算两遍。
（那次搜索只回了 2 个来源，所以那基本是它自己写的提议，不是查来的资料。）

**它真正给到的是环境类**，值得单独留一眼：裤缝对阴蒂反复磨（骑车、久走）、
久坐时椅面对会阴的持续压、内衣刮已经敏感的乳头、公交座椅的低频振动。
这几条不需要任何人动手 —— 一个人上学通勤坐一天，值就自己往上爬。

**它答的那个设计问题：几样同时来要相加，不能取最大值。** 理由是心理会放大身体的
敏感度，两条通路互相乘，取最大值严重低估。它另外建议心理＋身体同时在场时
再加 20-50% 协同，以及高位设软上限 —— 软上限我们已经有了（按比例衰减自己会停），
不用再加一层。

**⚠️ 相加这个方向对，但要补一句上限（2026-08-31 第四轮，用户口径，两份 PRD 共用）：**

> **同一档里的几件事就加起来；加过了上一档，就只算上一档，封在那一档的顶。
> 不同档的（一件大的＋几件小的）以大的那件为主，在它上面往上加一点。
> 加得上去，但最多顶一档，顶不了两档。**

这跟过载书 10 条是一路的：「同一件事做得更久、几样同时来，**档位自己就上去了**」
——「档位自己上去」就是进上一档，不是无上限相加。
没有这句上限，一场手嘴衣服全用上的性事能一轮把值堆到天花板。

乳意那份写在 `_乳意PRD.md` §四点五，**两边同一条规则**。

### 分档权重

性欲要**自己一张表**，不能复用 `METABOLISM_BAND_WEIGHTS`。
现有表低档是 `base:1, stimulus:0.5`（为尿意调的：憋着时外部刺激更容易挤出来）。
性欲要反过来 —— 低位心理有效、高位物理有效（墨墨平静期 心理 ×1.5／物理 ×0.5）。
**这句话本身就是保留「类别」这根轴的原因**：要执行它，系统必须知道这一份是心理还是身体。

实现：`METABOLISM_BAND_WEIGHTS` 从一张表改成按 key 查（`{ urine: {...}, libido: {...} }`），
查不到走中性值（base 1／stimulus 1／decay 0）。**不能写成
`key === 'urine' || key === 'libido'`** —— 两者方向相反。其余五项行为完全不变。
类别正好接在 `addMetabolismValue` 现有的 `source` 参数位置（tools.js:1885，
现在只有 `'base'`/`'stimulus'` 两个值，本来就是「这一份算哪一侧」的意思）。

### 旧验算已作废

原先那段（「非孕期约 11 分钟越过满线」「孕晚期约 1 分半到线上」）是在
「衰减只在停手时跑」的假设下算的。现在衰减每分钟都跑，那三行结论全部无效。
唯一留下的是最后那条：冷启动直接上手在低档物理权重打对折，效率不如心理
（墨墨这层说对了，保留）。

---

## 三、正文表现

**核心原则：高性欲不改变她做什么，改变的是她怎么做、以及她想把这件事往哪儿推。**

三层分开，不要压成一条「自主权递减」的轴（墨墨的错就在这）：

| 层 | 高性欲对它做什么 |
|---|---|
| 目标（她想干什么） | **增加**一个，不替换。她始终还在做要求她做的那件事 |
| 手段（她怎么做） | 往那个方向偏。这是真的行为改变，墨墨这层说对了 |
| 控制（能不能执行） | 只有超限区间才崩。崩的是执行，不是目标 |

**「超乎自己预想」的正确写法**（来自过载书 11 条）：她的想法一直没变，是身体先动了。
- 声音 2 档：开始有她没打算发出来的声音
- 身体 2 档：腿开始自己合或者自己开
- 语言 3 档：只剩词，而且往往和她真正的意思不一致
- 配合 3 档：**做不到不等于不肯** —— 她听见了、也想做，身体不接
- 22 条：身体在到，而主观上完全是另一回事

墨墨写成「行为选择几乎完全被本能驱动」= 想法被替换掉了，这是错的。
她的选择从来没被替换，只是不再被服从。

**墨墨那套写法本身没错，错在位置。** 「控制崩了、身体自己动」只在超限区间成立。
墨墨没有超限区间（值夹在 0-100），只能把这套提前搬到 80-90 用，看起来就像替换行为。

**可检验的判据：** 写完一句问「她放弃了什么吗？」
放弃了原本的事 → 错，不管在哪档。还在做只是做法偏了 → 对（线以下）。
还在做但做不好了 → 对（超限段）。

### 档位表现草案（进 `METABOLISM_BAND_NARRATIVE_GUIDE`，不进 `maybePushNeed`）

```
[性欲分档表现]
- 低档以下：不写。
- 低：分神一下就收回来，做的事不变。
- 中：有存在感但不构成动作——她注意到了，不影响她在做什么。
- 高：进入决策层——同一件事她会选更靠近的做法：坐得近一点、话题往那边带
  一句、多留一会儿。这一档行为真的会变，但变的是做法不是目标。
- 满：进入身体层——控制开始掉。目标和手段都没变，是执行走形：呼吸和力道
  不稳，话说不完整，动作偏。
- 爆：执行崩掉，身体自己插进来。她仍然没有放弃手上的事——「被本能接管」
  是错的写法，正确的是「她还在做，只是做不成了」。
- 高性欲不改变她做什么，改变的是她怎么做、以及她想把这件事往哪儿推。
  任何档位都不要写成她丢下原本的事去找刺激。
- 爆档不等于会到。到没到由系统判定并写进 notify——notify 里没有，就写她
  还压得住，只是代价很大；不要让一个拥抱或一句话顺势变成她主动越线。
- 满档以上一直挂着不下来，意味着这段时间她处理不了（场合、他人、手头的事）。
  要写的是「她脱不开身」，不是「她不想」。
```

**绝不进 `maybePushNeed`。** 那条链路产出「应优先使用 bsExcreteMetabolism 缓解」，
会把正文推成每轮一场性爱 —— 比臭意伴意的问题更严重。作者自己已经把尿意排除在外了。

### 低档在正文里写什么（照搬尿意那条已有口径）

`registry_config.js:33` 原句：「低档影响的是她打算做什么，高档影响的是她身体在做什么；
不要在高档就写出湿痕。」性欲直接沿用。

- 低档写：换了个坐姿、走神的时间比平时长、某句话听着觉得有点怪、洗澡多站了一会儿。
- 低档**不写**：湿、夹腿、忍。那些是高处才有的东西。
- **低档不是「什么都不写」，是只写在她心里、不写在身体上。**

「一直隔着衣服蹭」和「完全没刺激」的区别，不是值在涨还是在跌，
而是**长期挂在低档 vs 回到基线**。一天下来这两种状态的差别，
不在某一分钟，在她有多少时间是「有点不对劲」的。

### 「涨得动但到不了」的正文口径

不是没反应，是**一直有反应，但永远只停在心里那一层**。
同一件事（走路裤缝磨着）在三个时期的结果不同，靠的是停留点整条往上走，
**不是概率**：

| | 停留点 | 结果 |
|---|---|---|
| 非孕期 | 远低于满线 | 一天下来只在低档晃，永远到不了 |
| 孕中期 | 过满线、够不到门槛 | 一直挂在满档下不来，但到不了 |
| 孕晚期／临产期 | 逼近门槛 | 够得着 |

这就是「非孕期小概率、孕期越往后越容易」的实现——它是可复现、可预期的，
不用掷骰。**环境类单独一样，即使孕晚期也该差一点**：22 条写走路那段的结论是
「走得越多越**难受**」，不是「到」。真的过去得靠叠——一边走一边想着，
两笔加起来才够。（这一条只有在「分别报、然后相加」下才做得出来。）

**这一档到的时候是她没打算到的，而且很可能在外面。** 所以写法跟床上那次不一样：
先是站住不动、找个能靠的地方，然后才是身体那一下；事后第一反应不是舒服，
是怕刚才有没有被看出来。

### 三档高潮的正文表现（register 照墨墨 #88）

| 档 | 大概什么样 |
|---|---|
| 小 | 里面缩几下就过去了，几秒。声音不一定有，可能只是吸一口气。之后还能接着做事 |
| 中 | 一阵一阵地缩，十几秒；腿绷住、脚背绷起来；出声了，而且是没打算发出来的那种；完事有明显的松脱感，会想歇一会儿 |
| 大 | 缩得又猛又久；整个人弓起来又摊下去；出水明显；有几秒是空的，话都接不上；之后碰不得，一碰就躲 |

（孕期特有的联动——宫缩、渗奶——不在这张表里，它们各有孕周门槛，归第 ④ 步。）

**哪一档由隐藏累加器决定，不由到的那一下多猛决定。** 被撩了又撤、快到了又停，
累加器往上加；完整到过一次，清掉。所以：

- **磨出来的那一次只可能是小的。** 没人撩她也就没人撤，累加器全程是空的。
- **「大」是被撩出来的，买不到。** 22 条把反复撤叫「全书攒张力最有效的手段」。
  磨一整天也换不来。
- 小那一档的关键是**够不上「事件」的规格**：不掏空、不留不应期、值掉一点但还在高位。
  所以磨一整天可以到好几次小的，这是这一档该有的样子，不是漏洞。
  累加器也只扣一点，攒着的张力不会被白花掉。
- **到了不等于解决了。** 小的只掉一小截，基线又高，很快回到原来的高度
  —— 正是 22 条那句「她没有真正退下来过」。

**⚠️ 上面这条还不够——小档不是「不掉」，是净涨。** 下体体感 §11b 自称「分档真源」，
给了一个我们没有的机制：

> ### ⭐ 关键机制：小高潮排不掉深层充血，反而加重它
> - 小高潮只清掉外面那一圈的张力，**盆腔深处的充血一点没动**
> - 而每一次高潮都让盆腔**再充血一轮**（血流增加、组织更肿）
> - **所以小高潮越到越胀、越到越空**——不是心理上的不满足，是**物理上的净增加**

所以**基线反升有第二条路**：不只「到一半被撤掉」，**小档本身也抬基线**。
这解释了「一晚上到好几回却越到越空、还要升档、还要往里塞」。

**§11b 只分两档（小＝外部一碰就来／大＝内外一起＋时间累积），我们分了三档
——中间那档材料里没有对应物。** 待拍板：是并成两档，还是保留三档、把中档定义成
「大的未完成版」。（后半句的「内外一起＋时间累积」跟我们的「爆档待够 N 分钟＋累加器」
是同一件事的两种说法，这部分对得上。）

**结算三分支得到了独立验证。** §12＋§11b 那条「钝麻」是从另一个方向推出同一套：

> ⚠️ **「钝麻」的成因是排放**：是**完成**的高潮把表层耗到钝麻。
> **没排出来的那一次不往钝麻里加**，反而把那一份唤起**留在身上**
> ⭐ 所以「连续到过几回 ＋ 最后那次被打断在临界上」的结果不是钝麻，**是过饱和**……
> **这种状态下被碰不是「感觉很淡」，是受不了**

「完整到过＝有不应期」「被撤＝跳过不应期转高敏」两条都在里面，
**而且这份材料跟墨墨无关，是独立到达的**。

**但它给了第三种状态，我们现在没有位置：过饱和。** 不应期系数（乘一个很小的数）
表达的是「碰了没用」；过饱和是「碰了受不了」。两者都不是「感觉很淡」，但方向相反。
待拍板。

**不应期在孕期缩短**，理由是盆腔充血不消退（§12）。所以不应期系数的恢复时间
该随孕期缩短，不是常数。

---

## 四、加减清单与联动

### 加

- 三类刺激（想象／环境／身体接触，见 §二），按分钟。
- **压制是加，不是减。** 过载书 22 条：「越忍越容易到 —— 越紧张肌肉越绷，
  盆底跟着绷，而绷紧本身就是接近到顶的肌肉状态。」
  跟憋尿不一样：憋尿是被动涨，这是主动压制反向推。
- **痒**（外阴）：「外阴的痒和唤起共用同一条上行通路 —— 不是联想，是信号本身混了」。
  一个与性行为无关的增加源。
- 排卵期倍率抬高。

### 减

- 衰减（落回基线）。
- 经期倍率压低。
- **胎动不在这里，它不扣值。** 早先写在这一栏是错的，见下面「宫缩与胎动」那节。

### 结算三分支（过载书 22 条，比墨墨的两分支准）

墨墨的判据是「还在不在做」，这里的判据是**「有没有完整到过」**。

| 分支 | 值 | 副作用 |
|---|---|---|
| 完整到过 | 大幅落 | 有不应期（碰不得） |
| 半失败（表面忍住了） | **不落** | 宫缩＋渗奶照样发生，比到了更难受 |
| 到一半被撤掉 | **不落** | **基线反升**，跳过不应期，转成高敏 |

「反复撤：每一次都在充血基线上再叠一层」—— 基线是可以被事件推高的，不只随孕期走。

**「转轻」和「撤」是两件事，不能混：**

| | 是什么 | 走哪条路 |
|---|---|---|
| 转轻 | 手法还在，只是弱了 | 走速率。有上面那条硬约束在，值仍然净涨，只是涨得慢 |
| 撤 | 快到了的时候停手 | 走结算分支「到一半被撤掉」——值不落，**基线反升** |

所以**撤必须由剧情里明确的「快到了 → 停」触发，不能靠速率变小自动推断**。
否则「他放慢了动作」会被算成攒张力，方向正好相反。

顺带一个白拿的效果：基线被反复撤抬高之后，衰减的地板也跟着抬高了。
同样是完全停手，之前被撩过几轮的那次跌得少——她能落回去的那个底本身升了。
这正是 22 条「每一次都在充血基线上再叠一层」，不用另写机制。

### 联动

- **三处同时**（22 条）：到顶时子宫收紧 ＋ 下面涌出 ＋ 奶渗或喷，「藏不住」。
  → 宫内压、尿意漏出（可直接复用 `applyUrineLeak` + `URINE_STRESS_LEAK_CHANCE`，
  盆底收缩和咳嗽打喷嚏同类，**不需要新机制**）、乳意。
- **触发点是「每一次接近」，不是「越过 hard」。** 22 条：「催产素在每一轮
  接近时都释放了一部分」。所以联动挂在「进入超限区间」上。
- **推产程**：`libidoMultiplier = 1 + (libido/cap)*0.25`（tools.js:3639）方向是对的，
  跟 22 条「这一整条在往产程那边推」一致。**不要删，只要改成读高出基线。**
- **与延产矛盾**：22 条「想延产就别用这一条」。这是可以直接用的张力点。
- 膀胱充盈放大刺激权重（膀胱压着前壁）；高性欲短暂抬高尿意的 `hard` 线
  （交感兴奋抑制排尿反射）—— 这两条加上漏出那条会自己长出一条链：
  起来了 → 不觉得急 → 值继续涨 → 到顶时盆底收缩 → 漏。没有单独设计，落出来的。
- `applyMilkFromLibido`（tools.js:2150）**方向是对的，别去改它**：刺激上来会把奶往前推，
  她觉得更胀，值该往上跳（乳意 PRD §三点五 那句「尖峰＋低谷」的前半句）。
  漏不漏由乳意自己那两道线判（50 挤才出／100 碰一下就漏），不由性欲决定。
  新口径下才暴露的坑只有一个：值每分钟都在动（基线漂移＋衰减），
  必须只对**刺激事件**泌乳，不对基线漂移泌乳。另两处改动见 `_乳意PRD.md` §五。

### 入盆：满线上的第二个断点（世界书 20 条）

第 20 条三段里跟性欲直接相关的两句：

> 中间入盆：「▸性欲不消退，但每次高潮后接一阵发紧。……反过来，不太痛的那几次宫缩
> 在感受上和唤起模糊近似，**两者互相激发**。」
> 完全入盆：「▸性欲：**底噪反而更高**（盆腔更充血），可行姿势却更少。……
> 高潮时子宫一收、胎头被挤，坠胀猛地加重，快感里掺着「要掉出来」的坠恐。
> **想要更频、每次却更费劲。**」

**已定：**
1. **基线继续往上，孕晚期那条上升曲线在入盆处不拐弯。** 「底噪反而更高（盆腔更充血）」
   就是基线，跟 22 条同一机制的延续。
2. **满线在入盆处有个断点，不是平滑过渡。** 先例很硬：尿意的入盆是
   「整条曲线上唯一的断点」（[metabolism_config.js:55](scripts/metabolism_config.js#L55)），
   两条线从 64/84 直接砸到 **52/62**，余量只剩 10，理由是「胎头压住膀胱底」。
   性欲压的是同一块地方（盆腔充血到顶、下面被顶满），所以照做。
   「想要更频」就是这个：桶浅到极致，一点刺激就到满线。
3. **「每次却更费劲」不进性欲。** 同 §一.12——费劲是姿势与体力，归行动能力那一摊。
   性欲只管「想要更频」，不在值里预扣。
4. **读 `engagedCount`，不读 `engagedDays`。** 两个字段名字像但不是一回事：
   `engagedCount` 是有几个胎头压住了（状态），`engagedDays` 是已经压住多少天
   （时长，只给分娩概率用，[labor_onset_config.js:74](scripts/labor_onset_config.js#L74)）。
   世界书说的是「压住了所以底噪高」，跟压了几天无关。照抄 `engagedDays` 会得出
   「入盆越久越想要」。**`engagedDays` 在全库出现得更多，容易搜到就抄，这是个坑。**
5. **多胎不额外算。** 照尿意那条注释：一个胎头压住就到顶，第二个挤不进同一个骨盆入口，
   压的还是同一个膀胱／盆腔，所以二值，多胎差别走速率。

### 宫缩与胎动：都不扣值，只进正文口径（这一节推翻了本文件早先两处写法）

**先纠两处我写错的：**
- §四「减」里那条胎动打断，写成了扣值——**不对**。过载书 11 条原文是「这一下会把她
  从任何档位上**暂时**拽回来」，我漏了「暂时」。它说的不是值掉，是过载书自己那栏
  「注意力」临时退一格（1 档两件事都在／2 档时间感先坏／3 档只剩当下）。
- 早先把插件宫缩三档（发紧／成串／先兆）对应成素材的 BH／前驱，**那个对应是错的**。
  插件那三档是**宫压值的显示档位**，而宫压只有两个来源：每天掷一次的前驱发作
  （[prodromal_config.js](scripts/prodromal_config.js)）和憋尿到爆档。
  素材说 BH「孕中期起一直伴随、日常反复出现」——**那个在插件数值层根本不存在**，
  孕中期宫压是躺在基线上不动的。所以「BH 那一档 → 加性欲」没有东西可读。

**已定：三样都不改值，全部落在 `METABOLISM_BAND_NARRATIVE_GUIDE`。**

理由是它们都不改「她打算做什么」，只在身体层占一下——正好落在尿意那条
「低档影响的是她打算做什么，高档影响的是她身体在做什么」的**中间**。

| | 程度 | 写法 |
|---|---|---|
| 胎动 | 最轻 | 感觉到了，愣半秒，接着弄。高档时那一下最突兀——她注意力全收进身体里的时候，从**里面**来了一下不是她的动作 |
| BH（发紧） | 中 | 肚子发硬，愣一下，可能停手几秒等它松开，然后继续。比胎动多一层「要不要等一下」的犹豫，因为它是整块硬起来、不是一个点 |
| 前驱成串 | 真停 | 素材原话「一阵接一阵、腰骶发酸，**得停下来扶着缓**」。这一档有插件现成的发作系统在跑（2–6 小时、8 小时冷却） |

**好处：连报的入口都不用加。** 不改值 → 模型爱写就写，写了不影响数值层 →
素材那三十种触发（跑步、爬坡、搬东西、翻身、排便使劲、打喷嚏、大笑、揉胸、
便秘肠绞痛……见 [孕肚体感.md](../../../06_体感/孕肚体感.md) §6 七类表）
**一个都不用实现**。早先「要给宫缩加事件源入口、要改插件」那条撤掉。

**胎动比宫缩常见得多，而且这个比例不用我们定。** 素材那张表同一个动作两边分开写：
跑步＝胎动几乎一定／宫缩看孕周；爬坡＝常有／偶尔；翻身＝常有／偶尔。
而且胎动**还有一整类独占**（§6.5 吃完东西、喝冰的、嘈杂环境、她安静下来、光照、
轻轻摸肚子，明确「一般不触发宫缩」），宫缩独占的只有肠道那一类。
频率是**按动作**给的，不是一个总体比率——所以写进 prompt 文本，不写进代码。

**「初期影响大、之后转钝」这条只对头胎成立。** 判据现成，不需要新概念：

| | 头胎 | 生过 |
|---|---|---|
| 第一次遇到 | 不知道那是什么，会真的停下来确认、可能担心 | 认得出来，一开始就是愣一下 |
| 之后 | 转成愣一下 | 一直是愣一下 |

字段都在：`experience.pregnantExperience`、`naturalBirthExperience`、
`surgicalBirthExperience`（[tools.js:3188](scripts/tools.js#L3188)），
加上孕周（第一次出现多久了）。**不写「她早就习惯了」**——素材 §10 那句是角色向的，
一个生过三个的角色不该在孕中期被一次 BH 惊到。同理不写次数
（「一天大大小小好多次」是她的频率），写「日常会反复出现」。

**前驱不套这条脱敏，它的曲线是反的**：一次次「狼来了」之后她学会的是
「会缓解的就是假的」，结果真临产时认不出来（通用书 20 条明写）。

### 唤起 → 宫缩：只做这一个方向，不闭环

**唤起 → 宫缩**已经有一半在了（高潮那条路上宫压 +2，
[tools.js:4428](scripts/tools.js#L4428)、[4437](scripts/tools.js#L4437)）。
但它挂在挂起的 `maybeTriggerOrgasmOvulation` 里——**拆那个函数时这条要留下**，
高潮引出宫缩是对的，不该跟着排卵一起被扔掉。
留的时候**要挂孕周**：[孕肚体感.md](../../../06_体感/孕肚体感.md) §6.8 第一条
「孕周是最核心的因素，催产素受体全孕期持续增加，早期可能完全没反应、晚期一碰就紧」，
现在那个 +2 是死的。

**反方向（宫缩 → 唤起）不做。** 上面那节已经把宫缩的作用定成正文口径、不改值，
所以这条自然就没有了——顺带避开了正反馈环，不用拿不应期去当阻尼救它。

**性欲压不住宫缩，这是结构性的，不是我们没接。** 宫压的「抬地板」机制
（[tools.js:2357](scripts/tools.js#L2357)）取 `Math.max`，只往上顶，没有向下的口子。
注释写了为什么：「写成抬地板而不是每小时加一笔：后者会跟回落打架，
加得比落得少等于没加，多了又无限累积。」

**宫颈成熟度是第二条独立的轴。** §6.8：「宫颈没成熟时宫缩再频繁也不会变成真临产；
宫颈成熟了→同样强度的宫缩才能推动产程」。所以「频不频繁」和「能不能推产程」
是两条轴，插件里两个都在（宫压＝强度，`engagedCount`／孕周＝成熟度），不需要新字段。

**膀胱那条链是两条并行的，不是一条**：我早先只写了「膀胱充盈放大刺激权重」，
§6.8 说的是膀胱充盈**放大宫缩**（而且不只概率、还有性质：「来了比空膀胱时更不舒服」）。
两条都成立。

### 产程期间的性欲：三段窗口（底层照通用书 30 条，放大层照过载书）

**这一节整段重写过。** 早先我照 30 条原样搬，搬进了两处毛病，见末尾「作废的」。

#### 三段的形状（保留，这是地基）

顺序固定、非单调：**能到 → 到不了 → 不由自主地到**。
三段是三种不同的东西，中间那段是关节——30 条原话：
「它在，第三段才是另一种东西；它不在，第三段就成了第一段的延续。」

**中段的边界不看 cm，看生理**：
- 起点：宫缩强度上来、间歇短到不够她放松（儿茶酚胺开始当家）。
- 终点：宫口全开、开始不自主往下用力（换成机械压迫那条通路）。

落到插件：**要判两层，不能只看阶段名。**
活跃期／过渡期＝中段、第二产程＝第三段，读 `stage`＋`pregnant.laborPhase` 就够；
**只有潜伏期跨在①②之间**，靠 `pregnant.laborHours`（当前 phase 走了多久）分早晚。

#### 纯生理部分（先分清，虚构加在这上面）

**结实的**：催产素链（高潮／乳头刺激→宫缩，临床在用）；内啡肽全程升、分娩到峰；
门控理论（非疼痛输入在脊髓压低痛觉传入，机制确凿）；
**中段自主神经是反的**——生殖区充血靠副交感，而活跃期儿茶酚胺很高、方向相反还压制催产素；
Ferguson 反射（胎头撑开→催产素涌出→不自主用力），**被压到的是阴部神经和盆神经，
跟高潮的传入神经是同一批**。

**弱的**：门控那支专讲触压觉，「唤起本身镇痛」另有一支（Komisaruk 系列）但实验做的是
非分娩状态；「高潮式分娩」有回忆式问卷报过，机制讲得通但发生率数据质量差。

**生理没说的（就是被夹带进来的）**：她会不会想到用、有没有习惯可迁移、
那条四段弧线（要反复试才走得出来，是行为不是生理）、她怎么解读。

**中段不可得的理由升级了**：从「太痛了」换成「副交感被压着、充血起不来」。
这个更硬，而且顺便说明为什么那一段该是最狠的一档。

#### 虚构方向：往性欲那边推（用户口径 2026-08-30）

**放大层用过载书，不用通用书**——通用书是底层照现实资料写的，过载书是放大层，
本来就是这么分的。

**中段：不是「空白」，是「顶满了却到不了」。** 过载书 11 条现成的那一格：

> ⭐**锁死在最高位**：内啡肽把痛压下去的同时，**底噪也被锁在最高位下不来**。
> 刺激量远超她平时能排解的，而**出口被堵着——到不了**。这一格比痛更难写也更有分量。

**它连激素都点对了**——中段的生理正是内啡肽爬到峰。不是硬套，是同一件事两种写法。
三段的关节没坏反而更好：原来那个空白没内容可写，这个有。

**我们的结构现成就能做**：停留点顶在门槛以下（本来是给环境类那条路设计的）
——基线推到最高、门槛也推高，值贴着天花板下不来。**不用新机制。**

**娩出段：撤掉「不经过唤起」那条锁。** 撤了它这条就直接走性欲值——
胎头压迫是一个刺激源，值照常涨、门槛照常判，只是刺激来自里面。
过载书那一格在等着：

> ⭐**身体和主观脱节**：她身上在完整地反应（该紧的紧了、该出的出了），
> 而主观上已经完全不是那件事了。

**痛不撤**：过载书本身说痛和舒服是两条独立的线、**可以同时满**。两条都拉满，不是拿一个换。
插件里这两条线正式产程期间都读得到（性欲值 ＋ `pregnant.laborPain`，
后者按 phase 给区间、着冠那一档是 8~9，见 [tools.js:3598](scripts/tools.js#L3598)），
所以这一段不用新字段。**但别把这句推广到孕期**——`laborPain` 只在产兆前驱与正式产程存在，
临产期发作期间那条痛线其实是空的，原因见 §6.7。

**这么定顺带把两个结构缺口都消了**：不需要「系数硬零」（中段不再是零）；
娩出段有家了（就在性欲值里，不用另建产程侧的事件系统）。**反而比原来省一套东西。**

#### 最后阶段：高频、居高不下（不用新机制，规则自己长出来的）

「爆档连续 N 分钟、期间刺激没断」这条规则碰上这一段的特点——**刺激断不了**：

| | 刺激的连续性 | 30 条怎么说 |
|---|---|---|
| 下降段 | 断续（进两退一，一松劲缩回去） | 反复很多轮 |
| 着冠前停滞 | 几乎不断 | **最集中** |
| 着冠 | **完全不断**（最大径抵住不再退回） | 撑到最开 |

**分界的判据是「还退不退回去」**，插件读得到（胎头下降度那根轴，
`fetal_descent_config.js` 分了能被顶回去和深固定）。这两段数值形状不同，别当一段。

同时三样到极值：基线最高（盆腔充血到顶）＋满线最低（入盆那个断点）＋刺激在最重档以上
→ 停留点远在门槛之上，**爆档是常驻状态不是偶尔碰到**。

两个后果：
- **不应期成了唯一的节流阀**（平时限频率的是「刺激停了」，这里停不了），
  所以间隔 ≈ 不应期 ＋ N 分钟。而不应期在这里最短（充血最重），频率自然最高，不用另给数。
- **不会被「钝麻」卡住**：§11b 那条 3~4 次上限**只管小高潮**（外部、表层），
  明说大的那一档「不受那个上限管——它排的是深层充血，而深层充血在孕期始终是满的」。
  胎头压迫走的正是深层那条。**所以「多次」是材料允许的，不是我们硬开的口子。**

#### 幅度可推（30 条自己批的四条轴）

> ·幅度可推：从**间歇闪现**推到**反复出现**；从「高潮样」推到**真的到顶**；
> 可带潮吹、失禁样喷溅、盆底连续痉挛。**高敏体质、延产的超大胎头、双胎的两次娩出**
> 都是往上推的合法理由。

做成一个旋钮，形状照泌乳档（注册时定、用户能改、模型不能动），
四级＝无／闪现／反复／到顶。世界书列的那几条「合法理由」插件里都有现成字段
（超大胎头、双胎、延产），当往上偏的加权。**默认保守**（无或闪现），用户自己拨上去。

#### 作废的（别再翻出来）

- **「①当镇痛工具用……旧经验还迁得过来」＋那条四段弧线** —— 这半句是从角色向文档
  夹带进来的。判据是**格式**：30 条那节每条都留了条件（②「再敏感的体质也没有空间」、
  ③「高敏体质…是合法理由」、副作用条「看角色懂多少，本条不规定」），
  **只有①没留**。跟第 11 条同一个毛病（邻居句都是条件句，只有性欲那句是硬断言）。
  **一节里只有一条不留条件，那条基本就是搬来的。**
  通用的只是机制（门控加内啡肽确有短暂效果、高潮＝一剂催产素）；**她会不会去用，
  是角色和剧情的事。**
- **由它推出的「要不要复用累加器承载那条递减弧线」** —— 整条作废，
  它是为一个不该预设的行为建机制。
- **「不要为它改性欲值的结构」** —— 这句我没经用户同意就写了，撤。
  而且往性欲方向推之后结论正好反过来（娩出段就该走性欲值）。
- **「着冠期反而递减」和那句「一路往上反而假」** —— 撤。那是照现实资料写的判断，
  而放大层的口径是可以不那么真实。更要紧的是**它跟过载书自己的地基冲突**：
  递减的机制是「灼裂感盖过一切」＝痛把别的挤出去，而 11 条那句自称「本书最要紧的一处」
  说的正相反——痛和舒服是两条独立的线、可以同时满。所以那个递减在放大层里站不住。
  去掉它得到最强的一点：**着冠那一段两条线同时拉满**，而过载书对这种状态配好了写法
  ——「档位越高这两条越分不开，到 3 档她自己也说不上来那是哪一种。
  **说不清比说得清更有分量。**」
  **去掉它也不会变成一条直线往上**，因为剩下的起伏是解剖给的、跟真实性口径无关：
  下降段本来就是「进两退一」；胎头娩出后有一个强制的停顿（几十秒到一两分钟，
  宫缩暂歇、肩还在里面）。形状是：下降段一波一波 → 着冠前停滞持续最猛 →
  着冠两条线同时满 → 滑出那一瞬炸开 → 停顿 → 肩膀出来。
  顺带：既然不用为可信度留坑，那个幅度旋钮的上限可以拉得更高。

**已定的一条**：第一段「每次高潮把产程推快」跟
`libidoMultiplier`（[tools.js:3639](scripts/tools.js#L3639)）是同一件事，方向对，
那处继续留、只改成读高出基线多少。

### 坠恐：进正文口径，不进数值

位置在 `METABOLISM_BAND_NARRATIVE_GUIDE`（跟尿意、宫缩同一块）。

但那块有条现成规矩要守（[registry_config.js:22-25](scripts/registry_config.js#L22)）：

> 「这里给每一档配写法，但只写强度与行为影响，**不写成因**——成因（是否怀孕、
> 是否入盆、哪一胎压着）由模型自己从已经看得见的状态里读，写死「胎头顶着膀胱」
> 会让没怀孕的角色在高档时出现胎头。」

所以不能写「入盆后高潮会有坠恐」，那是成因。写成条件句让模型自己看状态：

> - 高潮那一下是子宫整体收缩，所以**里面有东西压着骨盆的时候**，收缩会把那个东西
>   往下挤——坠胀猛地加重，快感里掺着「要掉出来」的感觉。不是疼，是怕。
> - 这一档不改变她想不想要，只改变她**到的时候是什么表情**。

「里面有东西压着骨盆」这个写法一句话覆盖三种情形、不用分支：没怀孕的角色读不到这个条件
自动不触发，入盆的最强，孕中期只轻微。也不违反那条不写成因的规矩。

第 20 条那句是这个机制最重的版本，不是另一个机制，**所以 §三 不用为入盆单开一套高潮写法**。

---

## 五、面板显示（这一条对七项都适用，是现存尿意就有的问题）

**病症**：条形图画的是「值占满线的多少」（`fill = value/urge`），已经是相对的、对的。
旁边那个数字显示的是**裸值**。于是孕晚期满线降到 64 时，条形说「满了」、
数字说「64，好像不高」。一个相对刻度一个绝对刻度，读的人被数字带跑。

**改法**：`displayValue` 从裸值换成百分比，且**分两段**，各自归一化。

- **线以下**：到满线的百分比，0% → 100%
- **线以上**：换成「余 X%」——超线余量还剩百分之几

例：非孕期值 120（厚度 50 用掉 20）→「余 60%」；入盆时值 62（厚度 10 用完）→「余 0%」。

**为什么线以上不能继续用同一把尺**：两条线的比例本身随阶段在变。
超线厚度占满线的比例 —— 非孕期 50%、孕中期 35%、孕晚期 31%、入盆后 19%。
所以最高能显示到的百分比是 150% / 135% / 131% / **119%**，
「120%」在非孕期是还剩一大半、在入盆时根本到不了。
同一句话在这里也成立：**用一把尺同时钉住标尺和天花板，两条线的比例会被绑死。**

**总原则**：面板显示的那个数，必须**就是分档依据的那个量**。现在尿意坏在这两者不同。

**性欲的分母不一样**：档位读高出基线多少，所以百分比是
`(当前值 − 基线) ÷ (满线 − 基线)`。孕晚期基线 55、满线 80 时，
她在 55 显示 0%（安静坐着，面板不喊），被碰到 68 显示 52%。

完整读法：条形填满 = 到满线；进第二段颜色 = 在忍；数字从「%」变「余 %」= 刻度换了；
档位名在悬停提示里兜底。

---

## 六、要改的地方

### 6.1 两份 `getLibidoCap` 是删掉，不是修

- `index.js:2355` `getLibidoCap(stage, profile)`
- `scripts/tools.js:2190` `getLibidoCap(profile)`

签名不同、公式一样，前后端各一份。这正是 `metabolism_config.js` 开头那段注释
写的病：「在别处硬编码 150 或复制键数组都会造成『加载时按一套算、运行时按另一套算』
的静默错档，历史上就是这么埋下的。」

改法照尿意：线表进 `metabolism_config.js`，`getMetabolismCap`（tools.js:1856）
加一个 key 分支（现在只有 `key === 'urine'`），前后端各自 import 同一份
（index.js:48 已经在这么做了）。两份实现一起删。

**删之前先看清它到底在算什么——它跟新设计量的不是同一样东西。** 现在五行：

```js
function getLibidoCap(profile) {
  if (!isTruePregnancyStage(stage)) return 100;
  const months = Math.floor(effectivePregnantDays / 28);
  const progress = Math.max(0, Math.min(10, months)) / 10;
  return Math.round(100 + (150 - 100) * progress);      // 100 一路升到 150
}
```

三处对不上：

1. **语义**：它抬的是**天花板**（把桶加高，孕期能被撩到更高的分数），
   而我们要的是**桶变浅**（基线往上、满线往下，从她平时待的位置到满的距离变短）。
   桶有多高不重要。所以不是在它这条线上挖个早孕的坑就完事，它量的本来就是另一样东西
   ——**这是现在插件跟新设计最大的一处分歧。**
2. **形状**：单调只升不降，没有第 10 条那段下凹。
3. **粒度**：`Math.floor(days/28)` 月步，全孕期只有 11 个数。下凹只占前三个月
   ＝只有 3 个数可用，「开始降／降到底／往回升」各占一个，出来是三级台阶不是曲线。
   这一段得按周或按天。

**按天这件事不用新机制，它自己就在按天算**（读 `effectivePregnantDays`，只是取整成了月）。
顺带白捡一条：`effectivePregnantDays = pregnantDays × 种族妊娠速度`
（[registry.js:1205](scripts/registry.js#L1205)），是**折算成人类当量**的天数，
所以曲线挂它上面，五倍速那种种族自动跟着压缩，不用为通用性再写一层。

**用 `effectivePregnantDays`，不要用 `fertilizationDays`。** 后者在着床时被清零
（[tools.js:1720](scripts/tools.js#L1720)、[1729](scripts/tools.js#L1729)），
只是着床前那段窗口的计数，不是孕期进度。

**曲线不能挂在阶段上，必须纯按天。** 插件的孕早期是 84 天整＝12 周
（[stage_config.js:15](scripts/stage_config.js#L15)），但回位可以在 16 周
——凹陷会跨过阶段边界。任何 `if (stage === '孕早期')` 的写法都会在第 85 天突然弹回去，
出一个台阶。照它现在那样只看天数、不问阶段。

**隔壁 `getUterinePressureCap`（tools.js:2202）跟它五行几乎一模一样、只换数字**，
改的时候顺手看要不要一起抽。

### 6.2 新口径下会失效的四处（都是读绝对值或读「值/上限」比值）

| 位置 | 现在 | 基线一抬会怎样 |
|---|---|---|
| tools.js:4451 | `currentLibido < libidoCap` 才排卵 | 顶满从「攒够了」变成偶发瞬间 |
| tools.js:4456 | `base.libido = 0` | 违反落回基线；孕晚期归零不可能 |
| tools.js:4560 | 伪孕要 `libido >= 50` | 孕中期基线可能就 ≥50，门槛废掉 |
| tools.js:3639 | 产程 `×(1 + libido/cap*0.25)` | 基线高＝长期挂着加速 |

全部改成读「高出基线多少」。

**其中 `maybeTriggerOrgasmOvulation`（tools.js:4441）整条挂起，实际动手前再定去留。**
它现在写的是三段，每一段都跟新口径冲突：
- 值到顶就算高潮 —— 我们定的是「在爆档待够 N 分钟」才算事件。
- 高潮 → 排卵 —— **因果反了**，该是排卵期让她更容易到，不是她到了就排卵。
- `base.libido = 0` 并 notify「性欲归零」—— 跟基线冲突。

而且它跟新定的排卵期倍率**打对台**：一个说「排卵期让她更容易到」，
一个说「她到了就排卵」，两个同时在，因果成环。
两个选项（**未定，动手前问**）：
1. 整条删掉。
2. 退成很弱的一条：只在排卵期窗口内、且是大的那一档才额外排卵
   —— 那样它变成「本来要排的被这一下提前了」，因果不再打转。
（诱发排卵在猫兔驼是真的，所以若保留要加种族门，挂 `orgasmOvulationAmount`。）

**已定的只有一条：任何一档高潮都不清零，落回基线。**

`shouldEnterPseudoPregnancy`（tools.js:4553）是另一种毛病，不是因果反了，
是**门槛失效**：要 `libido >= 50`，而孕期基线本身可能就到 50，那道门等于永远开着。
改成读「高出基线多少」即可，同宫压。

### 6.3 `base.libido` 要从 base 字段清单摘掉

`state.js:802`（字段名）、`:820`（sanitizer）、`:1025` 和 `:1843`（两处初始值）、
`registry.js:1277-1279`（注册清洗）、`tools.js:166`（schema）。
没有老存档，不需要迁移。

### 6.4 搬进 metabolism 带来的三个净增成本

1. **`immune.metabolism` 总开关会连带冻结性欲。** `addMetabolismValue` 第一行
   `if (!delta || profile?.immune?.metabolism || ...) return 0`。现在性欲不受它管。
   「代谢免疫」的角色顺带变无性欲 —— 要么接受，要么给性欲开后门。**待定。**
2. **五个衍生种族的豁免名单要表态**（血族／不死／魔导／变异／序列）。
   豁免的实现是 `metabolism[key] = 0` 强制归零，对性欲等于「无性」，语义大概对。
   **暂缓 —— 只讨论人类。**
3. **面板 `METABOLISM_DISPLAY_ORDER`（index.js:2528）加 `'libido'`**，
   同时删掉 index.js:3034 基础数值那行的「性欲」。~~那个数组还被四处 filter：
   阻塞抽奖（3603）、加速与扩容（4025/4045/4089/4131）。~~
   ~~「性欲被阻塞」是什么意思不明显，这一处要单独看，可能得把显示顺序和可阻塞键拆开。~~
   → **已消解（TASK-10）**：阻塞／加速／扩容四处的 filter 全删了，
   「性欲被阻塞」这个概念不存在了，数组只剩显示顺序一个用途。

### 6.5 文本

- `tracker_prompt_context.js:54` 现在只有一行 `'- libido: 性欲。'`，
  换成五行分档（约 200 字符）。长文放注册页，不放 tracker
  —— 实测 tracker prompt 里没有心理那六档的描述，那是注册专用的，
  所以在 tracker 写长档位文本是净增重量。
- `registry.js:793` 初始值说明「若角色开场就在发情、催情、强欲状态，可给较高值」
  —— 有基线以后这句含义变了（给的是绝对值还是高出基线？），要重写。
- `registry.js:803-807` 五个种族示例里的 libido 数字（12/35/28/20/60），
  口径变了会误导，要跟着调。
- 档位表现进 `registry_config.js` 的 `METABOLISM_BAND_NARRATIVE_GUIDE`
  —— 那块已经挂在 tracker prompt 里（tracker_prompt_context.js:6 引入、287 拼进去），
  是往现成的块里加，不是新开一处。
- **孕早期在 tracker prompt 里一个字都没有。** 系数是代码算的，
  但「这一段她不主动、被弄也起不来」得让模型知道，否则它照常写想要，正文和数值就对不上。

### 6.6 插件外：通用事件书第 11 条那句要改（不是插件的活，一起记着）

`世界书/孕期通用世界书/孕期通用世界书.json` 条目 uid **11**「早孕反应期｜孕4–12周」，
正文倒数第三段现在是：

> ▸性欲大幅压低，一周一两次甚至更少。这是孕期第一次「想要但身体不配合」。

出处是 [孕前期时间线设定.md:129](../../../孕前期时间线设定.md#L129)（角色向文档，
不该直接搬）。另有 12 个 `_backup_before_*.json` 和两处备份目录里有旧副本，都是快照、不用管。

**漏进去的是两样：**
- 「一周一两次甚至更少」——绝对次数，而且是**她的处境**卡出来的
  （宿舍六人间、没床帘、晚自习到熄灯只剩半小时，只有周末回家那一晚是窗口）。
  换个住单间的角色，同样的孕吐得不出这个数。通用书该写**相对量**：比她自己孕前明显低。
- 「这是孕期第一次……」——叙事进度声明，隐含前面有过「食髓知味那段甜」。
  通用书面对的角色可能孕前根本没性生活，说「第一次」等于替别人规定前史。

**而且原句自相矛盾**：前半「恶心疲倦让**欲望被按下去**」是新口径（欲望本身降了），
后半贴的标签意思反过来。不是通用书抄错，是那个标签从一开始就贴错了段
——「想要但身体不配合」形容的是**孕晚期**。

**改成（草案）：**

> ▸性欲整体往下走，压的是「想要」本身，不是只压住表达——恶心和嗜睡把念头一起按掉，
> 频率比她自己孕前明显低。这一段有没有拧劲取决于角色底子：本来性欲偏高的角色
> 念头压不干净，会出现「还想着、身体一点不接」；底子普通或偏低的角色就是单纯没兴致，不拧。

写成条件句是跟第 11 条自己的格式对齐——「小骨架、短躯干、腰腹本来就薄的角色，
到段末……」「罩杯往上将够一档（如 70C）」都是条件句，只有性欲这一句是硬断言。

**世界书和插件不接线**（用户既定规矩：世界书不对接插件，正则只打正文中文）。
这里也不需要接，需要的只是两边说的话不打架，而现在本来就不打架：
世界书管正文怎么写，插件管数值怎么走。

唯一值得「联动」的是**借它的周界**别自己另编时间：无感期 0–4 周（系数 1）→
早孕反应期 4–12 周（往下、到底、往回爬）。这是共用数字，不是接线。

**但世界书那个 4–12 不是曲线，是关键词搜索范围**——正文出现「孕 5 周」「孕吐」
才会命中，范围要写宽才不漏；插件那条曲线是这个角色实际第几周开始降，是个准数。
一个管「资料给不给模型看」，一个管「这个人第几周开始难受」，所以两边不必相等。
第 13 条那个 4～8 周的旋钮范围跟世界书的 4 周下限是一致的，不冲突。

**插件里也不需要早孕反应字段。** 全库只有
[tracker_prompt_context.js:167](scripts/tracker_prompt_context.js#L167)
一句 `hunger: 孕吐恶心、消化不良`，是让模型把恶心写进 hunger，不是可读的症状状态。
第 13 条那三个旋钮就够了。

### 6.7 顺手撞见的三处现存 bug（不是这次重写的活，先记着）

查前驱那套数值当摆位样板时发现的，**一个都没改**。列在这里是因为第 3 条会影响
§四 产程那一节的一个论据。

1. **`EPISODE_PAIN = 3.5` 是空转的，三重堵死。**
   - **写错字段**：[tools.js:3275](scripts/tools.js#L3275)、
     [:3309](scripts/tools.js#L3309)、[:3320](scripts/tools.js#L3320) 写的是
     `profile.base.laborPain`，而这个字段实际住在 `profile.pregnant.laborPain`
     （sanitizer 在 [state.js:838](scripts/state.js#L838)，两处初始值都在 `pregnant` 下）。
     全库**没有任何一处读 `base.laborPain`**；而且 `laborPain` 不在 base 的字段白名单里
     （[state.js:798-808](scripts/state.js#L798)），序列化时会被清掉。
   - **面板不显示**：痛感徽章的门是
     `stage === '产兆前驱' || LABOR_STAGES.includes(stage)`（[index.js:3088](index.js#L3088)），
     而可逆发作只在**临产期／逾期**跑，两个都不在门里。
   - **模型也看不到**：`getPromptFacingLaborState`（[tracker.js:484](scripts/tracker.js#L484)）
     同一个门，非产程阶段整个返回空对象。

   所以「发作痛 3.5，跟真前驱足够接近所以她分不清」这个设计意图**在数值层没兑现**，
   现在全靠 notify 那三句话在撑。
2. **`nightMultiplier: 1.5` 是有意的死代码。** 注释自己写了「留给引擎按时辰用，
   没有时辰概念时不生效」——**插件确实没有时辰概念**，全库只有这一处提到时辰。
   顺便替乳意那份的「夜里最满只进正文口径、不挂机制」补上硬理由：不是不想做，
   是没有可读的时辰。
3. **`laborPain` 只在产兆前驱与正式产程存在。** 所以 §四 那句「痛和舒服是两条独立的线，
   插件里现成就有两条」**只在正式产程成立**；临产期／逾期发作期间那条痛线是空的
   （见第 1 条）。产程三段窗口本来就只发生在正式产程，所以那一节的结论不受影响，
   **但别把「两条线都现成」推广到孕期**。

---

## 七、待拍板

**方向类（这两条定了，方向就齐了，剩下全是数字）**
- [ ] **模型是分别报三类，还是只报一个总数。** 只有分别报，「一边走一边想着」
      才能比「只是走着」高一点，环境类那条路才走得通；相加也才成立。
      代价是模型每轮要多算两笔。
- [ ] `maybeTriggerOrgasmOvulation` 是整条删，还是退成「排卵期＋大档才提前排卵」
      （见 §6.2，**动手前问**）。
- [x] ~~产程中段那个「彻底空白」怎么表示~~ **消掉了**：中段改成「顶满了却到不了」
      （停留点顶在门槛以下，现成机制），不再需要系数取 0。见 §四。
- [x] ~~娩出段那条非自愿反应放哪个子系统~~ **消掉了**：撤掉「不经过唤起」那条锁之后
      它就走性欲值，不用另建产程侧的事件系统。见 §四。
- [x] ~~第一段那条「镇痛效果递减」的弧线要不要复用累加器~~ **整条作废**，
      它是为一个不该预设的行为（她会拿这个当止疼办法）建机制。见 §四 作废清单。
- [ ] 娩出段那个「幅度」旋钮的四级刻度（无／闪现／反复／到顶）与默认值，
      以及超大胎头／双胎／延产各偏多少。**形状已定（照泌乳档），量未定。**

**数值类**
- [ ] 非孕期 `urge` / `hard` 的实际数值（我提过 100/150，**未确认**）
- [ ] 衰减比例（旧提案 20%/分钟，已知有两个疑点，见 §二）
- [ ] 三级各自的区间上下界 + 给模型看的例子（例子要压两头和中间）
- [ ] 「先加还是先减」的顺序 —— 它决定停留点公式里的 K 是 4 还是 5
- [ ] 高潮门槛的 N（连续几分钟）
- [ ] 累加器的加减量
- [ ] 不应期系数的起始值和回到 1 需要多久（中档／大档各一套）
- [ ] 心理＋身体同时在场要不要再加 20-50% 协同（grok 建议，独立问题）
- [ ] 孕期要不要那个 1.2 量级的全局倍率
- [ ] 经期倍率、排卵期倍率（黄体期／滤泡期要不要动另说）
- [ ] 孕期各阶段的基线与线 —— 两张表，要用速率反推
      （做法同尿意：先有「入盆后约 40/小时」，才推出「余量只留 10 正好一刻钟」）
- [ ] 孕早期下凹的三个旋钮各自的默认值与允许范围（起点 4～8 周是方向已定，
      具体默认几周、深度压多少、回位多少周都**未定**；见 §一.13）
- [ ] 下凹段用按周还是按天插值（月步已确认不行，见 §6.1）
- [ ] 入盆那个断点的两条线具体数（方向已定＝往下砸，量未定；照尿意用速率反推）
- [ ] **高潮分两档还是三档。** 下体体感 §11b 自称「分档真源」但只分两档，
      中间那档材料里没有对应物（见 §三）。并成两档，还是保留三档、
      把中档定义成「大的未完成版」。
- [ ] **过饱和要不要做成第三种状态。** 不应期＝碰了没用，过饱和＝碰了受不了，
      方向相反（§三 引 §11b）。现在只有前者。
- [ ] 小档抬基线的量（方向已定，见 §三；跟「被撤」那条的量要一起摆，否则两条会打架）
- [ ] 不应期恢复时间随孕期缩短的曲线（方向已定：孕期充血不消退所以更短）
- [ ] 高潮→宫压那个 +2 挂孕周之后的曲线（§四 已定要挂，量未定）

**待联想／已列但建议不接的**
- [ ] **孕早期下凹的横轴：纯按天，还是读 `acceleration.sleep`／`blockage.hunger`。**
      读症状字段的话「天数不一定可靠」就彻底没了——她哪天吐得凶那天就压得深；
      代价是那两个值由模型每轮填，比曲线更飘，跟「一定要剧情明确要求才会变」有张力。
      **未定，这是唯一需要拍的一条。**
- 泌乳（`applyMilkFromLibido`）：现在只在性欲**涨**的时候给奶，孕早期性欲在降＝自动不给。
  跟世界书第 11 条「全孕程胸胀痛最凶、一碰就疼」方向撞不上，但**碰巧不出错**。记着别去改坏。
  **补（2026-08-31）：不是碰巧。** 第 11 条那个胀是腺体在长、跟奶无关，本来就不该走乳意
  （`_乳意PRD.md` §五 末、§九点五 单开一块 guide）。**而这条联动的方向也是对的**
  ——性欲涨→乳意涨，只要改成只认刺激事件即可。反过来那个方向（乳意→性欲）用户
  2026-08-31 说先不做，待议形状记在乳意 PRD §五 末。
- 臭意（`blockage.odor`＝阴道分泌物增生）↔ 性欲：生理上直接相关，
  但分泌物在我们这套里属**环境类刺激**的一部分，接了就是双向环。**建议不接**，
  只在正文口径里让两边别互相打脸。
- 伴意 ↔ 想象类刺激：三类刺激里唯一有现成代谢键可靠的，但容易变成「孤独就发情」。
  **建议不接，或量级极小。**
- 行动能力 ↔ 孕晚期／入盆：两个值都在，不用接线。只要正文口径知道该同时看它们
  （世界书 44 条「姿势的可行性·肚子放在哪里」）。

**语义类**
- [ ] `immune.metabolism` 是否连带冻结性欲
- ~~[ ] 「性欲被阻塞」的含义，以及要不要把 `METABOLISM_DISPLAY_ORDER`
      和可阻塞键清单拆开~~ → **已消解（TASK-10）**：可阻塞键清单随三签一起删了
- [ ] 线以上的显示用「余 X%」还是「满 +X%」（分母都是超线厚度）
- [ ] 种族豁免名单（**暂缓**）

---

## 八、口径澄清（会话中修正过的）

- **孕晚期性欲继续升不是「偏离现实」。** 「想要」（心理欲求）孕晚期可能降，
  「起来」（唤起阈值）是降的也就是更容易。性欲值量的是后者。
  过载书 22 条给了物理依据：「盆腔充血是常驻的，所以底噪是物理性的半唤起。
  从开始到临界的距离比常人短得多。」
- **通用性是判据，不是「我的角色卡用不用得上」。** 数值曲线挂在现有种族参数上、
  人类取 1.0 当中性点，非人类自动落出来。不硬编码人类数字。
- **种族名录每轮都发，498 字符**（`buildRaceCatalogBlock`，
  tracker_prompt_context.js:289，注释自己写着「每轮都发」）。
  有现成开关 `settings.raceCatalogInPrompt`（面板 `bs-bt-race-catalog`），
  只用人类的话关掉基本无损。生理参数（数值）只发在场种族那一份，这部分没问题。

### 我提过又改掉的（别再翻出来重论）

- 「轻/中/重按不间断／躲不开分」—— 错。持续多久已经由分钟数管了，
  再按那个降一档等于时长扣两遍。第二根轴是**多用力、多快**。
- 「道具＞插入」—— 那是我例子写歪了，不是口径。手、口、道具、插入在同一根轴上。
- 「轻的直接接触」归**中**，不归重。
- 「一直弄也到不了」—— 说反了。停留点停住只是值不再往上，她还待在高处；
  高潮看的是**在最高档待了多少分钟**，不是值还在不在涨。
  真正的「到不了」是停留点落在门槛以下。
- 「孕早期只压系数、基线只往上不回头」—— 错。孕早期基线也要往下，
  否则出来的是孕晚期那张脸（值不低、只是碰她没反应）。见 §一.11。
- 「用 `fertilizationDays` 当孕期进度」—— 错，它在着床时清零。用 `effectivePregnantDays`。
- 「这是这套里第一个要按天插值的东西」—— 错，`getLibidoCap` 现成就在按天读，
  只是取整成了月。照它的写法改粒度即可，不用新机制。
- 「早孕反应的起点可以写死，因为人跟人差别不大」—— 错，用户口径是差好几周（6～7 周起，
  最早 4 周）。起点也得是旋钮。
- 「世界书 4–12 周和曲线起点不一致，是个矛盾」—— 不是矛盾。那是关键词范围 vs 具体数，
  两回事，不必相等。见 §6.6。
- 「A（照设定填）和 B（模型自己判断）二选一」—— 摆错了，两者本来就是一条规则的两半：
  写了照写、没写在窄范围内自己挑。同「级给区间不给固定值」。
- 「全库只有一个 `getLibidoCap` 定义，PRD 里说两份那句不准」—— **我这句才不准**，
  当时只 grep 了 `scripts/`，漏了仓库根的 `index.js:2355`。§6.1 原来写的两份是对的。
- 「不要全局倍率」—— 说太满了。准确说法是**孕期不需要额外全局倍率**，
  不是这个旋钮不该存在（排卵期就该用它）。
- 「胎动打断＝扣值」—— 错，漏读了过载书原文的「**暂时**」。不改值，只进正文口径。见 §四。
- 「插件宫缩三档＝素材的 BH／前驱」—— 对应错了。那三档是**宫压值的显示档位**，
  而宫压只有两个来源（前驱掷骰、憋尿到爆档）；素材那个「孕中期起日常反复」的 BH
  在数值层不存在。所以「BH 那档→加性欲」没有东西可读。
- 「要给宫缩加事件源入口、要改插件」—— 撤掉。定成不改值之后，那三十种触发一个都不用实现。
- 「宫缩 ↔ 唤起做成双向，用不应期当阻尼」—— 撤掉。只做「唤起→宫缩」一个方向，
  不闭环，也就不需要拿一个机制去救另一个机制。
- 「前驱的分界是强度／是时间门」—— 两次都不准。前驱本来就 36 周后才可能被掷出来，
  所以不需要额外的门；而「能不能推产程」另有一条独立的轴（宫颈成熟度）。
- 「初期影响大、之后转钝」适用于所有人 —— 不对，**只对头胎**。
  素材那句「她早就习惯了」是角色向的。见 §四。
- 「§11b 那条小档不掉值」—— 不够。小档是**净涨**（小高潮排不掉深层充血反而加重它），
  所以基线反升有两条路，不只「被撤」那一条。见 §三。
- **「30 条那节可以整段照搬」—— 不能。** 它的第①条是从角色向文档夹带进来的
  （判据是格式：那节每条都留了条件，只有①没留）。跟第 11 条同一个毛病。
  **通用书也会有这个问题，不是只有素材才有。**
- 「着冠期递减、一路往上反而假」—— 撤。那是照现实资料写的判断，
  而且跟过载书自己的地基（痛和舒服两条独立的线、可以同时满）冲突。见 §四 作废清单。
- 「产程那两处是结构缺口，得为它们加东西」—— 往性欲方向推之后**两个都自己消了**，
  反而比原来省一套。所以遇到「做不出来」先问是不是方向定反了。
- 「孕期动三个旋钮」—— 实际主要是两个（基线、线），倍率是可选微调。
- 「小的那一档不进排卵、不进伪孕」—— 不对，那是给一条本来就要拆的机制打补丁。
  排卵那条整个挂起（见 §6.2），不做这个补丁。
- 「四类刺激各一个固定值」（心理3／环境2／隔衣8／直接15）—— 数值作废、混轴作废。
  留下来的是：那四行的前两行变成**类别**，后两行变成身体接触类内部的**档位**；
  3:2:8:15 这个比例形状可以当摆位起点。






