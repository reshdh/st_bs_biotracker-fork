# 乳意重写 · 设计记录

状态：**设计定稿，代码已落地。** 2026-09-04 全部拍板项落码完成（见
`trellis/TASK-06_乳意待拍板.md` 顶部拍板记录）：体质系数表（发育领先模型）、
喷概率表、排奶接宫压 +1~2、存量函数、满档溢出承接、反射 3 天、反锁恢复。
这份文档降级为设计存档——历史提案与推演过程，查「为什么」看它，查「现在跑什么」看代码。
最后更新：2026-08-31（第四轮。**新增 §四点五「加多少、减多少」，这一节是加减值的定稿。**
封顶到了就停、事件也推不上去；漏不减值，封顶＝平衡点；排一次不一定排空，按剧情三档落点；
常规排掉的门是「这段时间允不允许」而不是「够不够时间」；几笔一起来加起来但最多顶一档；
一段连续刺激只推一次。**§八 产后整块不做、断奶不做**——用户拍板，交给模型自行判断。）
上一轮：2026-08-31（第三轮。把作废的内容真删了，不再留划掉的旧文
——§三点八 原来那版停留点摆位、§九点五 那张旧草案分档表、§六 素材原文的第 2、3 条，
三处都只留结论和还能用的锚点。地基是 §三点六：六个档、一趟一趟爬、溢出两道线 50／100。
产量只看孕周和档位；孕早期面板读数＝0。）
建立：2026-08-30（从性欲值那轮讨论里分出来的——它自己是一整个子系统，
跟性欲的接口只有 `applyMilkFromLibido` 一条，合在一起会让性欲那次改动范围翻倍）
主参考：`世界书/孕期通用世界书/孕期通用世界书.json` 条目 **uid 37**
「泌乳量·闸的松紧」（**这一条就是设计稿**，不是素材——它自称「修正项·覆盖各阶段条的数字」，
本来就是照旋钮的样子写的）。
相关：`_性欲值PRD.md`（接口一条，见 §五）

---

## 一、这个值是什么

第 37 条开门那句是整份的地基：

> **泌乳＝腺体做了多少 × 这道闸漏了多少，两个都是变量。**
> 腺体从 16 周起就在生产，胎盘孕激素压住的只是最后一步「放量」。

**插件现在只有腺体那一半，闸那一半整个没有。**

而闸的个体差极大，第 37 条明写：

> 同样是没病的普通孕妇，这道闸的松紧个体差极大。有人到生都只有几滴，
> 有人二十几周就能挤出产后头几天的量——**这两头都在正常范围里，不需要任何理由。**

---

## 二、现状：插件里的乳意

三个来源，一条线都没有：

| | 位置 | 现在是什么 |
|---|---|---|
| 被动 | [tools.js:2145](scripts/tools.js#L2145) | 每小时 `0.08 × 倍率` |
| 周期 | [tools.js:2133](scripts/tools.js#L2133) | 黄体期 0.15/h、月经期 0.10/h |
| 性欲 | [tools.js:2150](scripts/tools.js#L2150) | `delta × 0.18`，排卵期另加 `delta × 0.05` |

倍率 `getMilkGainMultiplier`（[tools.js:1918](scripts/tools.js#L1918)）：
`(0.15 + 孕期进度) × (0.5 + fetalEnergyDrain + 胎数×0.15) × breedTolerance`

**满线／天花板：没有。** 写死 `0..150`，全阶段一个数。
对比尿意有 `URINE_LINES` 整张表加入盆断点（[metabolism_config.js](scripts/metabolism_config.js)），
乳意什么都没有。

---

## 三、四档（照第 37 条搬，量为足月前后·单侧单次挤出／两侧日总量）

| 档 | 单次 | 日总量 | 平时什么样 |
|---|---|---|---|
| 紧 | 2~5mL | 10~30mL | 到生也就几滴。平静时**永远不漏**，热水冲、揉、扯只冒一颗珠子，用力捏才成滴。**全孕程不用任何垫吸物** |
| **中（默认）** | 12~18mL | 100~150mL | 会渗、要垫，但还是「漏了一下」这种**事件** |
| 松 | 40~80mL | 300~500mL | 坐着不动也在往外走，**不再是事件是状态**。垫一个时辰就透、一天换十几次，衣襟从来没干过；侧睡压着那侧会被湿醒，早上贴身那层可以拧。反射一触即发——被碰到、想起来、甚至只是解开衣襟，先酥麻一下然后两边同时喷出去，能射一两尺 |
| 全开 | 150~250mL | 1.5~2 升 | 无时无刻都在流。**没有「漏了」这个事件，只有「现在湿到哪一层」**。垫几刻钟就饱和，睡一觉褥子那块是湿透的。自发渗 20~40mL/时/侧 |

**默认＝中档**（用户已拍板 2026-08-30，跟第 37 条「不选就是中档」一致）。

**这等于插件默认比现实高五到十倍，是有意的，不是没注意到。**
真实孕妇整个孕期挤出来也就几滴到一两毫升、多数人到生都不需要垫
——那正好是**紧**档。中档已经是产后头一两天的量。
所以这把尺子从现实一直铺到失控，默认针放在偏上的位置。
**以后觉得「默认太多、该往现实修」的那个念头是误修，别改。**

### 档不只改数字，它开关三套机制

| 门槛 | 从哪档起 | 开了什么 |
|---|---|---|
| 「憋」那一整套 | **松** | 沉紧→硬→发烫→跟着心跳跳着疼→硬块反锁（第 37 条明写「中档以下不写」） |
| 藏不住 | **松** | 胸口比肚子先破防：肚子有垂坠／顶起可做、有衣物可糊；一片持续扩大的湿印＋甜腥味**没有对应的遮法** |
| 性质翻转 | **全开** | 黄稠转白稀，把产后 2~3 天那个质变提前到孕期。**代价是产后那一跳没有了** |

所以**默认（中）不解锁任何新机制**，只是量比紧档大。真正长出新东西的门槛在松档，
而松档要用户主动拨。前三档孕期一律是初乳：黄、黏、稠，干在布上结硬痂。

### 启动时间各档不同

| 档 | 启动 |
|---|---|
| 紧 | 20~24 周，**也可能整个孕期一滴都没有、直到产后才来** |
| 中 | 14~18 周 |
| 松 | 早的 12~14 周 |
| 全开 | 看外力何时上，下限不早于 10~12 周（腺体得先长出来） |

孕前还在哺乳没断奶的那种从头到尾没停过，**不存在「第一次」**。

**插件现在的 `0.15 + progress` 从第一天就在加，等于所有人从受孕就开始泌乳。**

### 落到哪一档（这些是加权，不是判定）

- **往松偏**：双胎（胎盘更大、催乳激素更多）、延产（多出十几周激素预备期）、
  孕前还在哺乳没断奶又怀上的（腺体从没停过，闸对她本来就是半开的）、长期高频乳头刺激（受体上调）
- **往紧偏**：初次怀孕、孕程偏短
- **直接跳全开：只有外力。** 体质叠加最多堆到松档的顶

---

## 三点五、三层结构（这一节是地基，先看它再看别的）

乳意有三层，各层互不干扰。**混在一起想就会乱**（讨论时反复栽在这里）。

| 层 | 谁定 | 变不变 | 管什么 |
|---|---|---|---|
| **档**（闸的松紧） | 用户，注册时 | **定死** | 这具身体漏得多痛快 |
| **长期上升** | 孕周，自动 | 一路往上 | 腺体产得多少 |
| **临时触发** | 剧情事件 | 尖峰＋低谷 | 把存着的一次性倒出去 |

### 档：定死，不随行为上调（2026-08-30 拍板）

**注册时定，之后不变**，除非用户明确要求或剧情外力（第 37 条：「直接跳全开：只有外力」）。
模型不能改。

第 37 条把「长期高频乳头刺激」列成往松偏的**加权**——那是给用户判断用的依据，
**不是让插件自动累积**。这个习惯孕前就有还是孕期中途才养成，都在用户拨针的时候一并考虑，
不需要「习惯度」字段。

**理由**：一旦做成能被行为推，就等于开了个模型间接推档的口子，跟
「一定要剧情里我明确要求的情况下才会变」相冲。而且习惯真正改的那几样
（受体上调→反射一触即发／排空后回涨更快；乳孔撑开→多孔同时出）
**本来就是档位内部的参数**，拨针的时候自动跟上。

### 长期上升：随孕周，一路往上

腺体从 16 周起就在生产，产量随孕周单调上升，不用管。
**天花板是松档的顶**——体质和行为叠加都到不了全开，那一步只能靠外力。

⚠️ **刺激改的是闸，不抬天花板。** 素材两处都钉死了：「受产量上限约束、快不到哪去」、
「再往上就出界了」。所以刺激和孕周的关系是**乘的**（同一份刺激越到后期效果越大，
因为腺体那边的底数在涨），不是加的。而且腺体得先长出来，早于起始周刺激无效。

### 临时触发：改速率，不改总量（这条是硬口径）

> ⚠️ **一次排乳反射不会让她「多产奶」，它是把已经在腺泡里的量在几十秒内一次性推出去。**
> 所以在曲线上是**尖峰 + 紧跟一段低谷**，不是整体抬高。

**⚠️ 「尖峰」是先涨，别只记住低谷（2026-08-31 第三轮补，我自己读漏过这半句）：**

刺激上来，身体把奶从深处往前推。**东西还没出去，只是挪到了前面，所以她觉得更胀更紧、不是更松。
值往上跳。** 然后才看漏不漏：

| | 结果 | 素材原话 |
|---|---|---|
| **漏出去了** | 出去那部分让值掉下来 → 低谷 | 松档「被碰到、想起来、甚至只是解开衣襟，先酥麻一下然后两边同时喷出去」 |
| **没漏出去** | 值停在高处，**涨完不掉** | 紧档「热水冲、揉、扯只冒一颗珠子，用力捏才成滴」 |

**同一件事两个结果，差别只在闸。** 所以「排掉一笔」只是其中一种收尾，
不是这一层的定义——这一层的定义是**先往上跳一笔**。

漏不漏由乳意自己那两道线判（50 挤才出／100 碰一下就漏，见 §三点六），**跟性欲无关**。

**时间粒度：乳意用小时，性欲用分钟，两份不一样。** 渗漏是 mL/时、回涨低谷是一到几小时，
`passedHours` 够用（性欲那份必须改用 `deltaMinutes`，理由见 `_性欲值PRD.md` §二）。
**而排乳反射是几十秒的事，所以它在数值上不能是速率、只能是事件**——这跟「一次推出去」
是同一件事的两面，互相印证。

**插件现在 `applyMilkFromLibido` 就是「性欲涨就加乳意」，方向是对的**，
它做的正是上面那个「尖峰」。要改的是三处别的，见 §五。
（我一度写过「方向反了、高潮该是减一笔」——那是把「尖峰＋低谷」只读了后半句，已删。）

通用触发源（都是催产素脉冲，跟具体角色无关）：高潮、热水冲胸、碰到或扣到乳头、
**听到婴儿哭声／看到哺乳画面**、宫缩（前驱来的时候胸跟着渗）、无缘无故来一下。

**这一层由模型报事件触发，但不会被滥用——自限的**：排空之后紧跟低谷，
连报十次也没东西可排。

**回涨窗口随孕周缩短**（方向已定，具体数未定）：早期排空后要好几小时才回到蓄满，
晚期只要一两小时。所以孕晚期「刚排完那阵是软的」这个窗口越来越短。

**夜里最满**，而且不只是攒得久——催乳素在睡眠中本来就是全天生理峰值。
所以晨起那一次量最大。这一条不用挂机制，写进正文口径即可。

### ⚠️ 「奶不会攒」和「整个孕期越积越多」不矛盾

早先我把这两条读成冲突，**是我错了**。它们是不同尺度：
- 第 37 条那句「奶不会攒」管的是**几个时辰到一两天**（短期循环）。
- **长期趋势是一路往上**，那是上面第二层。

所以要同时做两条曲线：长期产量单调上升，短期在蓄满↔排空之间循环。
**插件现在两条都没有。**

**短期那条靠常规排掉兑现**（她能排的时候自己排了），不靠重吸收——素材原句后半
「滞留反而压低产量」已经删了，见 §六。

### 自发溢出：不是「涨到满线停住」，是抬地板

门槛是**孕周**，不是值：过了起始周，产出直接变成往外走，不再攒在里面
（第 37 条各档启动时机，见上文）。

形状用现成的——**宫压那个「抬地板」**（[tools.js:2357](scripts/tools.js#L2357)，
憋尿和前驱发作用的就是它）：到了自发渗漏那一档，值被顶在地板上，
排掉一次落下来，很快又被顶回去。

**量级有个现成锚点**：前驱发作那个地板是 26（[prodromal_config.js:49](scripts/prodromal_config.js#L49)），
在 150 的刻度上约 17%。那是「被顶住下不来」这个形状在本插件里唯一拍板过的数，
自发溢出的地板照它起步再按档调（松档往上、中档更低或没有）。

### ⚠️ 引素材时的判准（这一节栽过好几次）

第 37 条（通用）和 `06_体感/胸部体感.md`、`06_体感/孕肚体感.md`（角色向）
讲的是同一套机制，**但具体数字只在角色向那几份里**。所以一去找细节就会落进那份文件。

**判准：因果可以搬，周数和 mL 一律不搬。**

已经栽过的两次，记着别重犯：
- 搬了胸部体感 §1 那张分阶段 mL/时 表（16–20 周 <1mL → 过期段 125mL）当通用曲线
  ——**那是松档那一条**，通用书四档各有各的量。
- 拿「她从 16 周挤了二十多周才到那个位置」当「所以档位需要能被累积推动」的论据
  ——用一个角色的历史去决定插件机制。

---

## 三点六、六个档 ＋ 溢出两道线（本节是值这一层的地基）

### 值不是「她停在哪」，是「这一趟能爬到哪」

**她一趟一趟地爬**：排掉一次 → 掉下来 → 重 → 里面装着东西 → 一碰就漏 → 又排掉。
38 周照样跑这一圈，只是跑得快、跑得高。所以档给的是**这一趟的天花板**。

（第一版摆位把档做成四个固定的「停留点」，假设她坐在那儿不动。
那一节已经删了，残骸和还能用的锚点在 §三点八。）

素材有锚点：uid 38 说松档以上孕晚期「每天必须排十几次奶」
≈ 一个半小时一趟（从排空爬到满档）。量级上跟尿意入盆后约 40/小时 是一个数量级。

### 值量的是「撑」，不是「有多少奶」

一个数，量的是里面顶着多难受。所以非孕期的胸胀也有读数（下面黄体期那条），
孕期只是同一根尺子往上抬。**溢出量是另一笔账，不共享这个数**（见下面「溢出」）。

### 六个档（用插件现成的档名，不另起一套数字）

沿用 `getMetabolismLevel` 原生刻度：无 <25、低 25、中 50、高 75、满 100、爆 125，天花板 150。

| 档 | 她什么感觉 | 什么时候在这儿 |
|---|---|---|
| **无** | 软的，趴着不难受，蹭到没感觉。往上一点变成「说不上来」——内衣好像有点紧、睡一觉压得有点酸，要她说是什么她说不出来 | 卵泡期整整九天 |
| **低** | **重。** 比平时沉，趴着要先侧过来或垫个东西，跑起来下楼梯想用手臂压一下。乳头蹭到衣服有感觉——不是疼，是「有」。还是软的，躺下还会摊，外面看不出来 | 非孕黄体期的顶点。**孕早期不在这儿，那一段读 0，见下** |
| **中** | **里面有东西了。** 按下去有阻力，外上方和乳晕后面能摸到一块一块的，腋窝那片也觉得满（腺体有一部分往腋下走）。开始想去弄它——不是想要，是发痒那种冲动 | 有奶之后的日常。默认（中档）角色大部分时间 |
| **高** | 同上但硬得按不下去。乳头被顶着往外走更翘，可乳晕整片绷紧反而更难捏起来（同时发生）。皮绷得发亮、能看见皮下的青色。**进决策层**：穿哪件、外面够不够厚、多久处理一次、别让人碰到、不敢穿浅色 | 紧档到生也就到这儿；中档被碰／洗热水澡会顶到 |
| **满** | **碰不得了。** 低档是「蹭到有感觉」，这一档是蹭一下要屏一口气；抬手、弯腰、被撞、被抱都有反应。**这一档起有前兆**：乳晕后面先酥一下或像针扎，半秒到两秒，她知道要来了但挡不住。走路会不自觉用手臂护在前面（跟护肚子是两个不同的动作） | 松档以上常驻 |
| **爆** | **疼。这一档不看值，看在满档以上憋了多久。** 四段依次：沉得像挂了东西 → 硬得像塞了石头 → 发烫、跟着心跳一跳一跳地疼 → 整个上半身不敢碰，抬手疼、深呼吸疼、衣服搭上去也疼。前三段排掉就好，最后那段排不掉、越挤越出不来 | 松档以上且长时间没排。**中档一辈子到不了** |

**⚠️ 无档和低档必须留。** 她一年里绝大多数日子就在这两档，
最低一档要是「胸比平时重」，那她一年三百天都在胀。

### 非孕期：顶多到低档，一辈子

**不是我们定的，是插件现成的数算出来的**：黄体期 0.15/时（[tools.js:2135](scripts/tools.js#L2135)）
× 24 × **12 天**（[stage_config.js:10](scripts/stage_config.js#L10)）≈ **43**，
离中档门槛 50 差 7。**够不到。**

| 周期阶段 | 值 | 她什么感觉 |
|---|---|---|
| 卵泡期（9 天） | **清零** | 无档。什么都没有 |
| 排卵期（2 天） | 很低 | 还是无档上沿 |
| 黄体期（12 天） | 0 → 约 43 | 慢慢进低档，后半程胸重、趴着不舒服 |
| 月经期（5 天） | 0.10/时，来了之后开始退 | 还在低档，然后往下 |
| 下一轮卵泡期 | 清零 | 回无档 |

**四档旋钮在非孕期不参与**——紧档的黄体期和全开档的黄体期一模一样。
四档管的是奶漏得多痛快，没奶就没得漏。**这条不用写判断**，
是 `applyCycleBreastNeedGain` 不过泌乳倍率落出来的（[tools.js:2133](scripts/tools.js#L2133)）。

例外照插件现成的走：**假孕期 0.08 倍率**（[tools.js:1907](scripts/tools.js#L1907)），
微量泌乳型，够不到高档。素材没管这个，现成的数留着。

### 有没有奶：看孕周，跟档数无关（两件独立的事）

- 紧 22 周／中 16 周／松 13 周／全开 11 周开始有（§三 那张表）。
- 在那之前里面是空的，挤也挤不出来（她那一段照样胀得厉害，但那是长出来的，不是存出来的）。
- 在那之后，就算她刚排完在无档，里面也是有的，只是这会儿被掏空了。

**有奶之后变的是天花板，不是当前值。**

| | 这一趟最高能爬到 |
|---|---|
| 非孕 | 低 |
| 孕早期（还没到启动周） | **无（0）** —— 见下 |
| 有奶了 | 中 |
| 反射建立后 | 满 |
| 松档以上且憋久了 | 爆 |

**⚠️ 孕早期这格是 0，不是低档**（跟 §十 那条「孕早期那个胀不给值」对齐；
原先这里写「低 —— 跟非孕一样」，跟涨的规则打架，已改）。
理由：她已经不在月经周期里了，周期那条路不给值；启动周之前泌乳那条路也不给值。
**所以面板上是空的。她那一段胸最胀最疼，但那个胀整个走正文**，见 §九点五 那块单开的 guide。
两根尺，别在面板上混（§三点六 末「两根尺」那张表）。

**最终天花板 ＝ 孕周解锁到的那格 和 档能到的那格，取小。**
所以 §三 那张门槛表（湿痕／憋／藏不住 从松档起）**一行判断都不用写**
——紧和中的天花板落在湿痕那道线底下，够不到就是够不到。

**排乳反射是一次性开关，不是一格。** 跟爬到哪一档无关，跟孕周有关：
没建立＝压了才出、不压没有、**没有前兆**，「喷」那张表整个不存在；
建立之后酥麻前兆出现。**隔启动周多久没有依据，这个数要摆。**

### 溢出：只有两道线，其余全是结果

**判定就一条：过 100 就漏。** 剩下的全是「她停在哪儿」落出来的，不用另写规则。

| | 什么样 |
|---|---|
| **50 以下** | 一滴都没有。用力挤也挤不出来 |
| **50 以上** | **用力挤才出来**，自己不会漏 |
| **100 以上** | 不用挤了。**碰一下就漏，或者自己往外走** |

**「一直在漏」不是第三道线，是她一直停在 100 以上下不来。**

| 档 | 平时停在 | 于是 |
|---|---|---|
| 紧 | 高档以下（**封顶 75**） | 够不到线，**永远不漏**。素材：热水冲、揉、扯只冒一颗珠子，用力捏才成滴 |
| 中 | 高档（**封顶 95**） | 够不到线。不湿衣服，但夜里渗一点点、干了结成那块痂 |
| 松 | 满档（**封顶 125**） | 一直在线上 → **一直在漏** |
| 全开 | 满档往上（**封顶 150**） | 同上，只是更凶 |

**封顶到了就停在那儿，事件也推不上去**（2026-08-31 第四轮拍板）。
所以漏不漏只看封顶数在 100 的哪一边——紧和中在下面，永远不自己漏；松和全开在上面，到顶就一直漏。
紧和中的区别不在漏不漏，在**多久到顶**（18 小时 vs 4 小时）和**挤出来多少**
（一颗珠子 vs 产后头两天的量）。四档速率、残值、加减值全在 **§四点五**。

**渗和喷不是两套判定，是同一次漏的两种写法**：平时自己往外走的是渗，
被碰／热水／高潮那一下出来的是喷。判据只有一条：**有没有半秒到两秒的酥麻前兆。**
模型看有没有触发事件就知道写哪种。

### 「撑不撑得住形」不需要新字段

硬不硬、撑不撑得住形、乳头翘不翘，**全是同一个数往外的表现**——值高就硬，值低就软。
所以六档只需要一行总述，不用一档一行。

| | 平时（里面没东西） | 装满了 |
|---|---|---|
| 躺下 | 摊开、往两边塌、往腋下流，摸得到底下的胸骨；乳头朝斜上方 | **不摊**，自己撑着自己的形，乳头朝正上；按下去会顶回来，像装满水的袋子 |
| 坐站 | 平时的样子 | 往上抬、显得圆、下垂变少 |
| 侧躺 | —— | 压着那一侧会被挤（素材「压着那侧会被湿醒」就是这么来的） |

**⚠️ 这些是加在她本来的样子上面的，不是绝对值。** 本来软大的那种装满了也比小的软一些
——素材那条钉得很死：**闸的松紧跟胸的大小不相关，罩杯推不出奶量。**

### 孕早期那个胀：两根尺，它在「撑」上只到低档，在「疼」上是全孕程第一

素材 uid 11 原文列的全是疼和敏感（「一碰就疼、衣物一蹭就痛」），
而值量的是**撑**。所以不冲突。

| | 孕早期的胀 | 后期有奶的胀 |
|---|---|---|
| 撑（＝值） | **面板上是 0**（撑的那点东西不进这个值，见下） | 中到满 |
| 疼 | **一碰就疼、蹭到就痛** | 沉、硬，但不至于碰不得 |
| 想不想排掉 | **不想，也排不出** | 想 |
| 有没有出口 | **没有**，只能等它自己退 | 排掉就解 |
| 持续多久 | **好几周不断** | 几小时一趟 |
| 躺下 | **还是会摊**（组织长出来的，不是液体撑的），只是按上去更实更疼 | 不摊 |

**⚠️ 上一行原来写「低档」，2026-08-31 改成 0。** 撑确实不是零（胸围差 +1~3cm 是真的），
但**它不会变**——一整段几周一个样，给个不动的读数没有信息量，
还会跟「过 50 挤才出」撞上（那一段挤不出任何东西）。
所以那点撑整个走正文，见 §九点五 那块单开的 guide。

**最好用的分辨方法是看时间**：装满那种撑几个小时变一次、弄完就软；
长出来那种一整天一个样，第二天也一个样。

**一条自洽的检查**：后期真正疼过孕早期的只有反锁那一格，而**中档（默认）一辈子到不了**。
所以对默认角色来说孕早期真就是全孕程疼的顶点，**素材那句「全孕程胀痛最凶」字面成立**
——不是凑出来的，是四档门槛表和这句话自己对上了。

旁证：素材说孕早期**上下胸围差 +1~3cm**，真的在长大，所以撑不是零
——但撑起来的是组织不是液体。这就是它为什么读低档而不是读无档。

### 三条横着管所有档的

1. **刚排完那一段不是「回到无档」，是另一种状态**：比她平时还软、摊得比平时更开、
   有点空掉的感觉，敏感度掉下去，暂时不想管它。**要专门写，别接着写胀。**
2. **两边永远不一样多，固定同一边先。** 写的时候不是「胸胀」，
   是那一边先起来、另一边慢半拍跟上。
3. **值越高越撑得住形**（上面那张表）。

---

## 三点七、单位：值保持抽象，毫升只进配置层（2026-08-30 拍板）

**先破一个直觉：改成毫升并不能解决量级问题。** 17 周存 3mL 她已经觉得胀，
35 周存 3mL 什么感觉都没有。所以就算存的是毫升，档位还是得除以一条随孕周走的线。
**真正解决量级的是那条线会动，不是单位换成毫升**——尿意就是这么干的
（入盆时两条线从 64/84 砸到 52/62）。

**做法**：
- 四档的线表用毫升写、注释标明来源是第 37 条，代码里换算成 0..150 那套刻度。
- 渗漏速率同样（素材里是 mL/时），换算成「每小时加多少」。
- 面板还是显示档位与百分比，**不显示毫升**。

**为什么不把值本身改成毫升**（代价只有一个，但是真的）：
**非孕期的乳意没有毫升可言。** 黄体期胸胀、经期胸胀，里面一滴奶都没有。
字段若是毫升，非孕角色的胸胀就没法表示——同一个字段在孕期是体积、非孕期是感觉，
中途换单位。

### ⚠️ 「长出来的胀」和「存出来的胀」是两回事（口径，不因不拆字段而失效）

第 37 条自己分的：

> 跟 11 条那种胀分开：11 条「全孕程胀痛最凶」是**腺体在长、血流在涨，长出来的疼，
> 跟奶无关**；憋是**存出来的疼**，要到有量之后、松档以上才明显。

- **长出来的**：周期性胸胀（黄体期／月经期）、孕早期那个最凶的胀。**里面没有奶。**
- **存出来的**：泌乳的憋。松档以上才明显。

所以**孕早期那个胸最胀却一滴奶没有的情形，不该走泌乳这条路**。

**而这个字段现在装的本来就是两样东西，插件自己知道、只是存的时候分不开：**

| 路 | 位置 | 要不要过泌乳倍率 |
|---|---|---|
| `applyCycleBreastNeedGain` | [tools.js:2133](scripts/tools.js#L2133) | **不过**，黄体期 0.15/h、月经期 0.10/h，非孕也能涨 |
| `applyMilkGain` | [tools.js:2127](scripts/tools.js#L2127) | 过 `getMilkFetalLoad`，只有妊娠／假孕／产后才有 |

tracker prompt 已经给它们起了不同的名字（[tracker_prompt_context.js:162](scripts/tracker_prompt_context.js)）：

> 普通周期进入新一轮卵泡期时，**周期型乳意**会清零。妊娠、假孕或产后恢复的
> **泌乳型乳意**不会因跨周自动清除。

也就是说它已经要区分了，只能靠阶段猜该不该清零。

**不用拆成两个字段。** §三点六 把差别全放在**天花板**上
——非孕的天花板压在低档（黄体期 12 天顶多爬到约 43），孕期才抬上去。
**同一档给同一个感受，谁把它顶上去的不影响写法。**
而「靠阶段猜该不该清零」也不是将就：**它猜的就是天花板**
——天花板在低档的时候清零才有意义（本来就没爬多高）。

---

## 三点八、第一版摆位删了，这里是还能用的锚点

**原来这里有一版「档＝停留点＋回涨速率」的完整摆位（40／85／110／135 和 5／33／110／400）。
整节已删，因为前提就是错的**——它假设她坐在那儿不动，而真实的样子是一趟一趟地爬（§三点六）。
另一处错是那四个数从素材的 mL 日总量阶梯（1:6:20:75）缩放来的，
**那是产出量那根轴**，压成一个数就把「她多难受」和「湿了多少」绑死了，
违反 §一 的地基「泌乳＝腺体 × 闸，两个都是变量」。

**从那一版里保留下来、现在仍然成立的五条：**

1. **摆速率的锚点还能用**：素材 mL 日总量阶梯 **紧:中:松:全开 ≈ 1:6:20:75**
   （20／125／400／1750），加上 §三点五 那句「晚期排空后一两小时回到蓄满」。
   两个一起用就能反推每小时爬多少。**注意这个阶梯只准用来定速率，不准用来定值。**
2. **启动周那套不动**：紧 22／中 16／松 13／全开 11 周（§三 那张表）。
3. **按天插值、不挂阶段**（照性欲 PRD §6.1）：松档过 100 大约在 22 周＝154 天，
   跨在孕中期里，挂阶段会出台阶。
4. **加权项（双胎／延产／孕前哺乳 vs 头胎／孕程偏短）只动启动周，每项 ±1~2 周。**
   理由是这么做天然封顶——最多是「早几周到达本档的量」。
   要是让加权去动量，几项一叠就会越过全开，而 §三 明写体质叠加最多堆到松档的顶、
   全开只能靠外力。
5. **紧档那条「也可能整个孕期一滴都没有」不需要额外开关**：
   启动周填一个大于足月的周数，整条曲线就没启动过。
6. **面板百分比直接可比**（四档共用一条线这个前提没变，§十 语义类那条因此消掉了）。

**还有一条是那一版真正立住的地基，跟停留点无关，单独记在这儿：**

**一套线，不做四套线。** 如果四档做成四套线（紧档线低、全开档线高），
紧档一样会走到满档和爆档 —— 于是湿痕、憋、藏不住全都出现，
而 §三 那张门槛表明写着这三样**从松档起**。
反过来只用一套线（沿用 `getMetabolismLevel` 原生刻度：无 <25、低 25、中 50、高 75、
满 100、爆 125，天花板 150），档只改这一趟能爬到哪 —— 三个门槛自己落出来，
**一行判档代码都不用写**。
这也是尿意要六套线、乳意不要的原因：尿意的容量真的被胎头压小了，
乳意的档差在「漏得多痛快」，不在容量。

**一个还没解决的坑（从那一版留下来的，跟停留点无关）**：
全开档常驻在爆档区间，而现有分档权重表里爆档是 `base: 0`（身体不再自己攒），
跟素材「无时无刻都在流」冲突。**乳意大概要自己一张分档权重表**，跟性欲那份是同一个坑。

**⚠️ 原先这里还有一句「全开档一直在漏＝计数器一直被清零，所以它反而不容易反锁」，
那句是错的，已删。** 反锁的计数器**只有真排掉才清零，漏不清零**——不然松档全开永远不反锁，
而「奶太多没人给她排、堵住了」本来是现实里最常见的那一幕。
一直没人排的结局在 §四点五 末：奶退掉、封顶往紧档挪，不是无限反锁。

---

## 三点九、「到哪个程度会怎么样」——第二条线在时间轴上，不在值轴上

### 先答那个结构问题

尿意有两条线（`urge` 想去了／`hard` 收不住），中间那段厚度就是「还能憋多久」。
乳意**也需要第二条线，但它量的不是值，是时间**。

理由是两者的顶端方向相反：

| | 到顶了会怎样 | 所以第二条线量什么 |
|---|---|---|
| 尿意 | **漏出去** —— 身体自己把问题解决了 | 值：从想去到收不住还剩多少余量 |
| 乳意 | **反锁** —— 硬块、发烫，而且**越挤越出不来**（§六 第 4 条） | 时间：在高位待了多久还没排 |

所以尿意那条 `hard` 线在乳意这儿没有对应物：**值到本趟的天花板就不往上了**，
不会一路顶到 150。真正会恶化的是「停在高位这件事持续了多久」。

**⚠️ 这也是「乳意不该有爆档」那句的准确说法**（§六 末尾原来只说了半句）：
不是没有爆档这一格，是**爆档不由值的高低进入，由时间进入**。

### 三张表 ＋ 一个计数器（照尿意的分工，各管一件事）

尿意把「到哪个程度会怎么样」拆成三处，互不重叠（否则同一次判定会既算漏又算失禁）。
乳意照这个分工，内容换掉：

| 尿意 | 乳意对应 | 型 |
|---|---|---|
| 漏出量表 `URINE_LEAK_TIERS`（滴数／洇开直径） | **渗**：每小时湿多少、湿到第几层 | 持续，不掷骰 |
| 应激性漏尿概率 `URINE_STRESS_LEAK_CHANCE` | **喷**：被碰／热水／听到哭声时会不会真喷出去 | 事件，掷骰 |
| 憋不住必然失禁（`ratio²`） | **反锁**：憋着的时长到头 | 时间，不掷骰 |
| 排空残值比例 | 一次排乳反射排掉多少 | —— |

**分工的判据是 §九点五 已定的那条**：渗和喷不是程度差，是两套机制，
判据只有「有没有 0.5~2 秒酥麻前兆」。所以它们本来就该是两张表，不是一张表的两档。

### 表一 · 渗（按档给，不按值给）

**这张表的行是档，不是分档档位** —— 因为渗漏是「闸漏了多少」，那正是档的定义。
值只当开关：**过 100（满档）才常驻渗漏**，没过就只在被事件推上去那一下漏一次。

| 档 | 常驻渗？ | 渗多少 | 湿到第几层 |
|---|---|---|---|
| 紧 | 否 | —— | 平静时永远不漏；用力捏才成滴 |
| 中 | 否（天花板在 100 底下） | 事件推过 100 那一次：一处湿印 | 贴身那层，直径几公分，事后低头才发现 |
| 松 | **是**（天花板在 100 以上） | 素材原文 ≈20mL/时/侧 | 垫一个时辰就透 → 往外衣传 |
| 全开 | **是**（同上，更高） | 素材原文 20~40mL/时/侧 | 垫几刻钟饱和 → 外衣 → 坐卧的东西 |

**饱和传递照 §九点五 那条**：布吃满了不再扩大湿印，而是往下一层传。
所以「湿到第几层」是**渗量 × 时间 ÷ 当前那层的吸收量**，不是一张值→描述的表。
层数由叙事按当前衣着自己判断——照 `URINE_LEAK_TIERS` 那条注释的规矩
（「extent 只描述量与范围，不写载体：引擎不该替她安排」）。

### 表二 · 喷（掷骰，照应激性漏尿的形状）

尿意那张概率表按**分档档位**给（中 0.18／高 0.50／满 0.75／爆 0.90），
乳意照抄这个形状，但**乘一个档的系数**——因为素材说「自发喷通常只有几厘米」，
真的喷得远要三个条件同时到位（反射正在发生 ＋ 乳孔通畅 ＋ 有外力加压，§九点五）。

| 分档档位 | 有触发事件时会不会喷 |
|---|---|
| 无／低 | 不喷（还没到有量那一层） |
| 中 | 小概率，且只在有外力挤压时 |
| 高 | 中等 |
| 满 | 高 |
| 爆 | 几乎必然，而且两侧同时 |

**档在这里当乘数**：紧档整张表打到很低（素材：热水冲、揉、扯只冒一颗珠子），
松档以上接近 1（素材：「反射一触即发——被碰到、想起来、甚至只是解开衣襟」）。

**触发源清单已在 §三点五**（高潮、热水冲胸、碰或扣乳头、听到婴儿哭声／看到哺乳画面、
宫缩、无缘无故来一下）。这张表只回答「触发了会不会真喷出去」。

**射程不进表**。§九点五 明写射程由压力决定不由奶量决定，而且产前喷不远。
所以射程写进正文口径，不给数值。

### 表三 · 反锁（时间计数器，这是乳意独有的那条线）

**这才是尿意 `hard` 线的真正对应物。** 计数器只在两个条件同时成立时走：
**值在满档以上** ＋ **没有排乳事件**。一旦排了，清零。

递进照 §六 那条原文，**四段，写到硬块为止**：

| 憋了多久 | 什么样 | 可逆？ |
|---|---|---|
| ~4 小时 | 沉、紧，托一下比平时重 | 排掉即解 |
| ~8 小时 | 硬，摸上去有明显阻力 | 排掉即解 |
| ~16 小时 | 发烫，跟着心跳跳着疼 | 排掉即解，但要一会儿 |
| ~24 小时 | **反锁**：局部硬块、碰不得、**越挤越出不来**；整个上半身不敢碰、抬手都疼 | **不可逆递进**，要热敷软化＋轻手法 |

**这四个数只对松档以上有意义**——§三 那张门槛表明写「憋那一整套：从松档起，
中档以下不写」。中档的天花板在 100 底下，压根进不了满档，所以计数器不走。
**又是一条不用写开关的**：门槛由天花板落在哪一档决定。

**⚠️ 反锁跟其他三样的关键区别**：前三段排掉就好，反锁**排不掉**（挤压加重肿、路更窄）。
所以它不该由「排乳事件」清零，要单独的恢复条件（热敷、时间）。
这一格是唯一需要新状态字段的地方（`milkLockHours` 之类）；其余三段只是计数器的读数。

### 走时长的白捡好处

**计数器只管一件事——反锁。它不压产量**（那条已经跟着 §六 那两条一起删了）。
这么做还白捡两条：

1. **不掷骰，可预期。** 跟性欲那边「不掷骰、靠天花板筛」是同一个口径。
2. **回涨窗口随孕周缩短**（§三点五 已定方向）不用另做：爬升速率是随孕周涨的，
   所以晚期同样排空后回到满档更快，计数器也更早开始走。**同一个数管两件事。**

---

## 四、档位放哪、谁能改

**放 `bio`，注册时写一次。** 跟 `birthDifficulty`、`recoveryDays` 同货架，
形状照性欲那边刚定的三旋钮（`_性欲值PRD.md` §一.13）：有默认、设定里写了就照写、
没写用默认。**不是每轮让模型维护的状态，也不随行为累积**（见 §三点五）。

改的权限分三条，照插件已有的先例：

| 谁 | 能不能 | 依据 |
|---|---|---|
| 用户在面板里改 | **能，随时** | 天赋就是这个口径：[tools.js:349](scripts/tools.js#L349)「talents 对所有 LLM 工具均为**只读**……仅能由用户在外部界面调整」。泌乳档跟天赋同类（这具身体是什么样的） |
| 剧情外力（用药／术式／有人把闸拿掉） | **能，但走单独的口** | 第 37 条自己开的：「直接跳全开：只有外力」。这是剧情事件，该有专门的工具调用 |
| 模型顺手改 | **不给** | 用户既定：「一定要剧情里我明确要求的情况下才会变」。能每轮调的话写着写着就会自己往上爬 |

---

## 四点五、加多少、减多少（2026-08-31 第四轮拍板）

**这一节是加减值的定稿。** §三点六 定的是刻度和两道线，这一节定的是「什么让它动」。

### 门：不是「够不够时间」，是「这段时间允不允许」

排一次奶几分钟的事，**时间从来不是门槛**（我原先写成「够不够她排」，
还提了个「一趟以上才算跳」的门槛数——那个数不该有，作废）。判的是允不允许：

| | |
|---|---|
| 场景里连续推进（她在场、走不开） | **不排。** 值一路往上爬 |
| 剧情跳过去的那段日常（「两天后」） | **当她排过了**，落到残值，不出声 |

**于是四档在「隔两天再见面」那一刻就已经分开了**——落点按档给（下面那张表）。
最常见的开局就是隔了几天再见，那一刻松档已经在漏、紧档还是干的。
**插件不需要知道她在上课、在人前还是睡着。**

顺带两件事跟着落地：
- **「夜里最满」彻底交给正文**——插件不知道最后那一段她在睡觉。跟已定的「不挂机制」一致。
- **对照尿意**：尿意代码只看 `urine.holding` 一个旗子（[tools.js:2331](scripts/tools.js#L2331)），
  模型不标「她在憋」，连续推三小时也照样自动排掉了。**乳意这条口径比尿意现在的准。**

### 加·第一条：时间（底盘）

**只由孕周和档位决定，模型推不动。**

| 档 | 每小时 | 从排完到挤得出（过 50） | 到顶 | 一天几趟 |
|---|---|---|---|---|
| 紧 | 3.4 | 10 小时 | 18 小时 | 1~1.5 |
| 中 | 20 | 1.5 小时 | 4 小时 | 6 |
| 松 | 67 | 20 分钟 | 1.5 小时 | 16 |
| 全开 | 250 | 5 分钟 | 半小时 | 50 |

摆法：松档从排空爬到顶 1.5 小时（uid 38「每天必须排十几次奶」）＋ 阶梯 1:6:20:75，
残值取封顶两成。

**⚠️ 这张表是「没有任何刺激」时最慢的情况**，刺激是另外加在这上头的一笔。
中档光靠时间 1.5 小时到 50，中间被揉一把（+15）就四十几分钟到。
**紧档基本靠刺激活着**：光等时间大半天挤不出东西，有人一直弄她五个小时就到 50 了。
**紧档是「有人弄才有反应」，松档是「不用弄也一直在漏」——四张脸就是这么分开的。**

**⚠️ 全是足月的数。** 速率随孕周爬，启动周刚过那阵慢好几倍
（松档在二十六七周一趟要三小时，不是一个半）。

**⚠️ 中档是 20/时，不是早先随口给的 10。** 10 的话中档一天只 3 趟、
天花板要 7 小时才到，跟「产后头一两天那个量级」（新生儿一天吃八到十二次）对不上。

### 加·第二条：碰到胸

三档区间，模型按那一下**实际有多重**在区间里挑个数：**轻 3~8 ／ 中 8~20 ／ 强 20~35。**
区间不重叠，边界上就是同一个数（揉得最狠的那一下和吸得最轻的那一下本来就差不多）。

| 情形 | 加 |
|---|---|
| 隔着衣服蹭到、被抱住压到胸 | 3~5 |
| 趴着睡、侧躺压住那一侧 | 3~5 |
| 跑跳、颠 | 3~5 |
| 想到孩子、看到吸奶器 ※ | 3~6 |
| 揉整个乳房，没专门弄乳头 | 6~10 |
| 抱起孩子、看到孩子 ※ | 6~10 |
| 到了平时固定排的那个点 ※ | 6~10 |
| 洗澡热水直接冲在胸上 | 8~15 |
| 听见婴儿哭（谁的都算）※ | 10~18 |
| 乳头被摸、被搓、被捏 | 10~18 |
| 乳头被扯、被拉长 | 18~25 |

**※ 这四条是条件反射，要排乳反射建立之后才有**（启动周再往后一段，那个数还没摆）。
没生过、反射还没建立的，听见小孩哭什么都不会发生。孕前一直在哺乳没断的从头就有。

### 加·第三条：能把奶弄出来的那三样（同一个动作，两种结果）

| 情形 | 值在 50 以上 | 值在 50 以下 |
|---|---|---|
| 被嘴吸 | **排掉**（落点见下面减那一节） | 加 25~35，什么都不出来 |
| 用手用力挤 | **排掉** | 加 25~35 |
| 吸奶器 | **排掉** | 加 25~35 |

**吸奶器不是刺激，它直接就是排的动作**，跟被吸同一行。
紧档大半天都在 50 底下，所以这时候吸她也没用——只冒一颗珠子。

**⚠️ 过 50 不等于自动就排了**（这一句是用户补的，我原先写成「过 50 就是排」，错）：

| | |
|---|---|
| 50 以上 ＋ 有人吸／挤／用吸奶器 | 排掉 |
| 50 以上、没人动她 | **停在那儿。难受，但什么都不出来** |
| 100 以上 | 这时候才不需要动作，自己往外走 |

### 加·第四条：走性欲那条路（胸没被碰到的那些）

**胸一根手指没碰，只要性兴奋起来，奶一样往前走。** 催产素干的活，跟喂奶是同一个东西
（正常喂奶靠的就是它）。所以这不是勉强凑的类比接口，是同一个东西干的两件事
——这也是 §五 那句「方向是对的」能立住的原因。

走这条的：性交、口、下面用玩具、被亲、被摸别处、**性幻想**、**高潮**。

**一律：性欲涨多少 × 两成。不单独给数。**

**分界只有一句：碰到胸了走上面两张表，没碰到胸走这条。没有一条走两边。**

**⚠️ 「高潮」原先被我列在碰到胸那张表里算强档，已划掉**——它正是「胸没被碰、
性欲飙到顶」最典型的那一下，留在表里就是算两遍。划掉之后那张表就干净了：
只剩手、嘴、热水、孩子哭，全是真碰到胸或者条件反射。

**性幻想走这条**，而且它正好卡在已定的那条界上：只认刺激那一笔，不认基线漂移。
幻想是一次性的刺激，算；她一整天心里惦记着他、性欲基线整体偏高，那个不算。

**两成这个数**：代码现在是 `delta * 0.18`（[tools.js:2158](scripts/tools.js#L2158)），
量级对，原作者没错。**但它后面又乘了个孕周系数**，足月能到 1.9 倍、等于放大成三成半
——这是 §五 要改的三处之外新发现的第四处。

### 加·第五条：周期（只有非孕期）

| 阶段 | 每小时 |
|---|---|
| 卵泡期 | 0 |
| 排卵期 | 0 |
| 黄体期 | +0.15 |
| 月经期 | **−0.4**（现在代码是 +0.10，符号反了，见 §十） |

回到卵泡期清零。非孕期一辈子最多到低档（黄体期 12 天攒到约 43，够不到中档门槛 50）。

### 几笔一起来怎么算（2026-08-31 第四轮，用户口径）

**同一档里的几件事就加起来。加过了第二档，就只算第二档。**
**不同档的（一件大的＋几件小的）以大的那件为主，在它上面往上加一点。**

| 一轮里发生了什么 | 算多少 |
|---|---|
| 一笔轻的 | 3~5 |
| 几笔轻的（衣服蹭着＋抱住＋颠着） | 加起来，超了就按中档算，**封在 20** |
| 一笔中的 | 10~15 |
| 中的 ＋ 几笔轻的 | 15~20，够多就进强档，**封在 35** |
| 一笔强的 | 25~35 |
| 强的＋中的＋轻的一起 | 还是 35。**强档就是顶，再多也不动** |

**加得上去，但最多顶一档，顶不了两档。** 所以一场性事里手嘴衣服全用上，
最多也就算一笔强的 35，堆不出天花板。

**性欲折过来那两成也一起进这个筛子**，不单独走。
**一轮里所有往上加的笔，过同一个筛子，出一个数。** 只有时间那条底盘在筛子外面。

**⚠️ 我先前写过「几件事一起发生取最大的、不叠加」，那句是错的，已作废。**
加起来、加到升一档，跟「加」本来就是同一个道理。

**⚠️ 性欲那份 §二 写着「几样同时来要相加，不能取最大值」（[_性欲值PRD.md:297](_性欲值PRD.md#L297)），
方向跟这条一致，不用推翻——只要补一句「最多顶一档」。**
（那句话的来路 PRD 里当时就标了：是搜索时模型自己写的提议、只回了 2 个来源，不是查来的资料。
但它跟过载书 10 条「同一件事做得更久、几样同时来，**档位自己就上去了**」是一路的
——「档位自己上去」就是进上一档，我先前把它读成相加，错在这儿。）

### 一段连续的刺激只推一次

**不按次数算，也不按分钟乘。** 揉两分钟和揉半小时，一样。

理由不是嫌数据烦，是排乳反射本来就这样：它是把**已经在腺泡里的**那些量一次性往前推，
几十秒推完。推完就推不动了——里面的总量没变，只是挪到了前面。所以不该是十五倍。

**这条不需要任何机制，是白拿的。** 模型一回合报一笔，那一回合里揉了多久都是那一笔。
**「一轮报几下」这个问题根本不存在**：模型报的是这一段有多重，不报次数。

**⚠️ 我原先在这儿加了个「不应期十几二十分钟」，删了。** 加它是怕值被连着几笔刺激顶飞，
可**顶飞这件事封顶本来就管住了**。而且没了它也不出事：紧档一场戏被揉七八下，
最多到 75 就停，**到 75 照样不漏**（够不到 100）——
「胀得厉害、硬得按不下去，可一颗珠子都挤不出来」正是素材里紧档的样子。跑出来的结果是对的。

**⚠️ 两份 PRD 这一处故意不一样，别去「统一」**（说法也跟着改了，不是不应期）：
连续刺激不能一路直线往上涨，两边都得管住，但用的不是同一个东西：
- **性欲**靠衰减——每分钟漏掉一点，所以按分钟加也会越走越平，最后停住（「顶着但到不了」）。
- **乳意**靠封顶——到本趟天花板就停，事件也推不上去。它没有衰减这回事。

谁看见这两处不同就去对齐，会把其中一边弄死：给乳意加衰减，「奶不会攒」和「憋着」一起没；
给性欲加封顶，「顶着到不了」就没了。

### 减：只有两条，都是排掉

**排一次不一定排空。按剧情里实际排了多少挑**（用户口径）：

| 剧情里 | 落到 | 紧／中／松／全开 |
|---|---|---|
| 掏空了（喂完、抽完、挤到软） | 封顶两成 | 15／19／25／30 |
| 排了大半（喂到一半被打断、挤几下缓解） | 封顶一半 | 38／48／63／75 |
| 只出来几滴（挤两下看看、被吸了几口） | 减 5~15 | 都一样 |

**按比例，不按固定数。** 同一个「掏空一边」，全开档要拿掉 120、紧档只有 60
——这件事本身就不是一个固定的量。
**这也顺手否掉了 `bsExcreteMetabolism` 那个固定 −30**（[tools.js:2889](scripts/tools.js#L2889)）：
全开档一趟爬到 150，减 30 等于没排；紧档封顶 75，减 30 掉一半。同一个动作不该差这么远。

**白捡一条：一个值管两边，所以「只喂了一边」天然就是「排了一半」。** 不用给左右各开字段。

**残值当然在 50 底下，这不是矛盾。** 50 管的是「她能不能开始挤」，
排完停在哪是另一个数——**排空的意思就是「软了、挤不出东西了」**。
尿意已经是这个结构：门槛只用来判要不要去（[tools.js:2336](scripts/tools.js#L2336)），
落点是另一行、取门槛的一到两成（[metabolism_config.js:94](scripts/metabolism_config.js#L94)）。

第二条减就是上面那个门：**跳过去的那段时间允许她排，就当排过了，落到残值。**

**没有第三条。不衰减。** 不排就一直在那儿。

### ⚠️ 漏不减值（这条不写成减法）

松档封顶 125，到了就停在 125，一直漏。**漏是「停在 125 长什么样」，不是一笔减法。**
要是漏也往下减，就又变成又加又减了。**只有真排掉才落下来。**

于是**封顶那四个数不只是硬夹上去的盖子，它就是平衡点**：产的速度等于漏出去的速度，
值停那儿。代码上仍然是一行 clamp（一边 +67 一边 −67 等于没动），所以不会变成两笔账。

**白捡的：「她湿多少」不用另做一张表。** 平衡时往外走的速度就等于那一档每小时爬多少：

| 档 | 停在 | 往外走 | 长什么样 |
|---|---|---|---|
| 紧 | 75 | 0 | 干的，乳头上什么都没有 |
| 中 | 95 | 0 | 不湿衣服，但夜里渗出一点点、干了结成那块痂 |
| 松 | 125 | 67/时 | 一滴一滴，洇开一片，得垫 |
| 全开 | 150 | 250/时 | 走得快，会滋出去 |

松和全开差不到四倍，对上素材那个 400:1750 的阶梯。
**用户说的那三级（结痂那一点点／一滴一滴／直接滋）就是中、松、全开三张脸，不用另给数。**

紧和中停住的理由不一样：它们够不到 100、不漏，是**装不下了、腺体不再往里加**。
这跟第三轮删掉的「滞留压低产量」不是一回事——那条是会随时间漂的系数，这个只是个盖子。

### 不挤奶的时候什么时候降

| | |
|---|---|
| 非孕期 | **会降。** 月经一来往下退，回到卵泡期清零 |
| 孕期、场景里那几个小时 | **不降。这是故意的**——胀着就是不会自己过去 |
| 孕期、跳过去的那几天 | **降。** 就是上面那个门 |
| 剧情故意锁住她、好几天真排不掉 | **见下** |

**最后那一格不做成每小时掉一点**——那就是第三轮删掉的那个会漂的系数，
而且一做，「奶不会攒」和「憋着难受」一起没了。

改成一个剧情结果：**憋到最后一段之后还是没人排，值开始往下走，
但那不是松快了，是奶少了。** 降的同时封顶往紧档那边挪。

这条顺手解开一个死结：松档全开常驻在满档以上，按现在的规则会天天反锁、
永远反锁下去。有了这条，一直没人排的结局是**奶退掉、不再反锁**，而不是无限反锁。
（也因此 §三点八 末那句「全开档一直在漏＝计数器一直被清零，反而不容易反锁」**是错的**：
反锁的计数器只有**真排掉**才清零，漏不清零。不然松档全开永远不反锁，
而「奶太多没人给她排、堵住了」本来是现实里最常见的那一幕。那句已在 §三点八 划掉。）

### 一概不加的情况

- **启动周之前**（非孕期、孕早期）：摸也好幻想也好玩具也好，**面板不动**
  ——里面根本没东西。非孕期的值只从周期那条来。
- **乳夹、勒住、内衣、塑身带**：**不进这个值，只写正文。**
  它是一直压着，没有「一下」可以加；做成每小时加一笔就变成第四条上升路了，多一处会漂。
- **紧张、有人看着**：现实里会压住排乳反射（奶下不来），但素材里没这句，**先不做**。
  要做的话它是「这一笔不生效」，不是减值。

### 同一个数对四个档轻重差得远——这是有意的

中档一笔 20 相当于一小时的自然积累，紧档一笔 20 相当于六小时。

**手上那一下的力气不会因为她是哪个档就变小**，而且有封顶兜着，堆不出事。
这是后果，不是算漏了。

---

## 五、跟性欲的接口（只有这一条）

### 方向是对的：性欲涨 → 乳意涨（2026-08-31 第三轮拍板）

`applyMilkFromLibido`（[tools.js:2150](scripts/tools.js#L2150)）挂在
`options.libido` 上（[tools.js:4879](scripts/tools.js#L4879)）
——**模型每次报性欲变化都会顺带产奶**。

**⚠️ 我原来在 §三点五 和这一节都写了「方向反了」，两处都是错的、已划掉。**
刺激上来会把奶往前推，她觉得更胀，**值该往上跳**——这就是 §三点五 那句「尖峰」。
我把「尖峰＋低谷」只读了后半句，于是把高潮说成「减一笔」。

**漏不漏是乳意自己的事**（50 挤才出／100 碰一下就漏，§三点六），不由性欲决定。
紧档一样会跳、一样觉得更胀，只是东西出不来——素材原话「热水冲、揉、扯只冒一颗珠子」。
**所以联动只有一条：性欲涨，乳意涨。完。**

### 要改的是三处别的

1. **只认刺激，不认自己飘。** 性欲重写之后值每分钟都在动（基线漂移＋衰减），
   照现在这个写法她躺着不动、性欲自己飘一下就产奶。
   得改成**只对刺激事件泌乳，不对基线漂移泌乳**。
   （同一条也记在 `_性欲值PRD.md` §四。）
2. **还没有排乳反射的时候不加。** §九 明写启动那一段「纯机械挤压，压了有、不压没有、
   没有前兆」——没有反射就没有「往前推」这件事，所以这一段刺激不该让值跳。
3. **排卵期那个 `delta × 0.05` 删掉。** 第 37 条里找不到依据，插件自己加的，来源不明。

### 一件不因这次联动而改的

**这只是当下跳一笔，不会让她长期产得更多。** 长期产多少由孕周和档决定，档只有用户能改。
第 37 条把「长期高频乳头刺激」列成**用户拨针时的参考**，不是让插件自己累积
——§三点五 已拍板，**别因为这次联动重新打开那个口子。**

### 反过来那个方向（乳意 → 性欲）先不做

用户 2026-08-31 说先只看催奶这一边。记着待议的形状：满档以上她本来一碰就有反应，
所以那一档**身体接触类**刺激给的性欲比平时多（只乘那一类，不乘想象和环境，
也不动基线——胀不会让她凭空想要）；而爆档那一格是疼不是敏感，该反过来压性欲。
**绝对不做闭环**（性欲高→更敏感→性欲更高会自己转飞），跟性欲那份「只做唤起→宫缩一个方向」同一条规矩。

**孕早期的胀跟奶量无关，别接错。** 第 37 条把「胀」分成两种：

> 跟 11 条那种胀分开：11 条「全孕程胀痛最凶」是**腺体在长、血流在涨，长出来的疼，
> 跟奶无关**；憋是**存出来的疼**，要到有量之后、松档以上才明显。

所以通用书第 11 条那个孕早期胸胀不该走乳意这条路。
（这也解释了性欲 PRD 里那处「孕早期胸最胀却性欲在降、方向撞不上但碰巧不出错」
——不是碰巧，是那个胀本来就不是奶。）

---

## 六、「憋」那一层（松档以上才有）

递进：**沉和紧 → 硬 → 发烫 → 跟着心跳跳着疼 → 整个上半身都不敢碰、抬手都疼。**

素材列了四条，**中间两条 2026-08-31 整个删了**（理由见下）。留下的两条：

1. **排出去是立刻的、大幅的松** —— 这是唯一的出口。
2. **憋到头会反锁**：局部硬块、发烫、碰不得，而且**这时候越挤越出不来**（挤压加重肿，路更窄）。
   方向是热敷软化加轻手法。**写到硬块为止。**
   而且这是**不可逆的递进**：越拖越挤不出，越挤不出越疼，不会自己过去。

### ⚠️ 删掉的那两条，以及为什么（2026-08-31 第三轮，用户提的）

删的是：**「排了会更多」**（排空是需求信号，排得越勤量越大，正反馈）
和**「奶不会攒」的原理由**（没排掉的会被重新吸收，而且滞留本身压低产量）。

**两条一起删，不能只删一条**——它们是同一个机制的两个方向（排得勤→产得多／憋着→产得少）。
删一半就变成单向的，还剩一个系数在那儿漂。

**为什么删：**这两条都是「行为去推产量」，跟已经拍板的
**「档位注册时定死、不随行为累积、模型不能动」**（§四）直接对台。
§三点五 早就写了：素材把长期高频刺激列成**用户拨针的参考，不是让插件自己累积**。

**删掉之后产量只看两样：孕周、档位。两样都不是模型能推的。**
少两个系数，少一处会漂的地方。

**「奶不会攒」这个结论还成立，但换个东西兑现**——不是重吸收，是**常规排掉**
（照尿意 `applyRoutineUrineVoid`，tools.js:2329：没在憋的时候，越过「该去了」那条线就是去了，不出声）。
她能处理的时候自己就处理了。
于是**四档的四张脸来自「她什么时候没法处理」**（上课、在人前、睡着），
「夜里最满」也不用挂机制了——睡着就是没法排。
**「反锁」从每天都会发生降回剧情事件**，本来就该在那个位置。

**憋着仍然有代价，只是代价换了**：从「产得少」换成「疼」（上面第 2 条）。
素材写疼那一段比写产量少详细得多。
丢掉的那点反讽（憋的结果反而产得更少）进正文口径，世界书 uid 37 已经有了——**那需要一个场面，不需要一个数**。

**顺带消掉一句我曾经写过的错话**：「乳意不该有真正的爆档」。
爆档这一格要留，只是**不由值的高低进入，由时间进入**——值到本趟天花板就不往上了，
计数器在走。四段递进的时长（约 4／8／16／24 小时）在 §三点九 表三。

---

## 七、和宫缩的咬合（第 37 条自称「本条最有用的推论」）

> 乳头刺激与排乳 → 催产素 → 宫缩加强。松档以上，「必须频繁排奶」和「排奶会催产」
> 是**同一件事的两面**：孕晚期每天十几次排空，就是每天十几剂催产素。
> 宫缩更多、前驱来得更早更密。

**但推得动多少要看宫颈成熟度**——宫颈还硬的时候催产素再多也不进展。
这跟性欲那边刚定的「宫颈成熟度是第二条独立的轴」是同一条，两份 PRD 共用。

插件里 `catalyst` 那条链（催产素→宫压）现在只有高潮一个入口。
**排奶这个入口没有。** 松档以上开了之后它会成为比高潮更高频的催产源。

**如果接，必须做成一次性加一笔，绝不能做成抬地板。** 抬地板取 `Math.max`
（[tools.js:2357](scripts/tools.js#L2357)），一件一天发生十几次的事去抬地板，
效果等于把基线永久顶上去，而且会跟前驱发作那个地板 26
（[prodromal_config.js:49](scripts/prodromal_config.js#L49)）打架——两个都在时只听高的，
就读不出「这次发作让她更接近了」。做成一次性加一笔则会自己衰减掉，
量级也留得住（照高潮那条现成的 +2，[tools.js:4428](scripts/tools.js#L4428)）。

---

## 八、产后：不做（2026-08-31 第四轮，用户拍板）

**用户口径：产后恢复期以及后面这一块直接让模型自行判断，对产后不感兴趣。断奶不做。**

所以**这一节整个不实现**：不分档、下奶那一跳不做、初乳转成熟乳那个质变不做。
代码现在什么样就什么样（`getMilkFetalLoad` 产后恢复给固定 `1.35`，
[tools.js:1906](scripts/tools.js#L1906)；`canProduceMilk` 整个产后恢复期都产、
回到卵泡期那一下清零，[tools.js:2688](scripts/tools.js#L2688)）。
**这两处从「代码对不上」的清单里划掉了，它们现在是最终行为。**

素材里那几条留在这儿备查，别拿去实现：产后 2~3 天初乳转成熟乳、
日总量在原档基础上再乘二到三倍、各档产后每次 中 150~200mL／松 300~400mL／紧回到正常量级、
「紧档孕期少不预示产后也少」（两道不同的闸）。

---

## 九、各档共通的锁死项（第 37 条末尾）

- 量再大都要带代价：胀、硬、烫、跳着疼。那是压力的表现，跟着水量一起长。
- 排乳反射的顺序固定：**乳晕后方先酥麻或针刺 0.5~2 秒，然后才出来。**
  手法是压乳晕根部（C 形对捏、向胸壁压、向乳头推）；直接捏乳头出得少而且痛。
- **两侧永远不等量，固定同一侧先湿。**
- **这道闸的松紧和胸的大小不相关**：小的可以是松档，大的可以是紧档，罩杯推不出奶量。
- 启动那一段还**没有排乳反射**：纯机械挤压，压了有、不压没有、没有前兆。
  也还不到「挤得越勤越多」那一层（管道没通开，反复挤只会把乳头弄疼弄裂）。
- 最早的可见证据是**乳头孔上那块干掉的初乳痂**（黄褐、硬、发亮，像皮屑），
  它同时把孔堵住了——**抠掉之后反而更容易出，这是实打实的物理效果**。

---

## 九点五、正文口径（进 `METABOLISM_BAND_NARRATIVE_GUIDE`，跟尿意宫缩同一块）

那块 guide 有条现成规矩要守（[registry_config.js:22-25](scripts/registry_config.js#L22)）：
**只写强度与行为影响，不写成因**。成因（怀没怀、哪一档、几周了）由模型自己从状态里读。
所以下面全部写成条件句。

### 进 guide 的定稿（十二行，2026-08-31 第三轮）

**详细版在 §三点六，不进插件。** 这块 guide 每轮都要塞给模型，所以一档一行
——尿意十行、宫缩七行、前驱五行、漏出五行、便意六行，共三十三行，加乳意十二行是多三分之一。

```
[乳意分档表现]
- 无档：不写。
- 低：胸比平时重，趴着要垫一下，乳头蹭到有感觉。只有她自己知道，不构成动作。
- 中：摸上去不软了，按下去有阻力；开始想按一下挤一下。有存在感，还不影响决定。
- 高：进入决策层——穿哪件、外面够不够厚、多久处理一次、别让人碰到、不敢穿浅色。
- 满：进入身体层——蹭一下要屏气，抬手弯腰被抱都有反应，走路会用手臂护在前面。
- 爆：沉→硬→发烫跟着心跳跳着疼→整个上半身碰不得。这一档看憋了多久，不看值多高。
- 值越高越撑得住形：低档还是软的会摊，满档躺下不摊、自己撑着自己的形、按下去顶回来。
- 低档影响的是她打算做什么，高档影响的是她身体在做什么。
- 刚排过那一段写「空掉」：比平时更软更摊、敏感度掉下去，不要接着写胀。
- 两边永远不一样多，固定同一边先；不写「胸胀」，写那一边先、另一边跟上。
- 湿没湿由系统判定并写进 notify；notify 里没有就是没湿，不要自己让她漏。
- 有前兆（乳晕后面先酥半秒到两秒）才是喷；没前兆的是渗，事后低头才发现。
```

**最后两行就是溢出那整块的全部。** 模型不需要知道四档、也不需要知道 100 那条线
——照 notify 写就行。这跟尿意那条现成的规矩同一个形状
（「漏没漏由系统判定并写进 notify，不要自己让她漏」）。

**其余全部不进插件**：初乳什么颜色、乳头上那块痂、湿到第几层、喷不远、夜里最满
——世界书 uid 37 本来就写着，而世界书是按关键词触发、不是每轮都塞，插件重复一遍是浪费。

### 旧草案那张表整个删了，只剩两条结论

**删掉的是一张五行的分档写法表**（低／中／高／满／爆），
错在**少了无档和低档**——把「胸沉、里面有阻力」放在最低一档，
于是她一年三百天都在胀。现在的十二行在上面。

从那张表里活下来的两条：

1. **「低档影响的是她打算做什么，高档影响的是她身体在做什么」**（照搬尿意那条），
   已进十二行。
2. **湿的东西不是从「有没有」分，是从「常驻还是事件」分：**

   > **常驻的湿从满档起；高档是一次性的「漏了一下」，事后低头才发现。**

   原来写的是「中档以下不要写出湿痕」，太紧了——素材说中档「会渗、要垫」。
   准确的理由是**中档的天花板在 100 底下，被事件顶过线那一下才漏**（§三点六 那两道线）。
   中档那张脸就是「垫着，偶尔漏一次」。

**爆档那一行的口径也在这里定死**：它不是「值涨到 125」，是**在满档以上憋了足够久**
（§三点九 表三）。正文里爆档配的是憋的四段递进，写哪一段看憋了多久，不看值有多高。

### 孕早期那个胀：单开一块，别写混

档位那几行不能碰它——那块的规矩是**不写成因**，而「孕早期会胸胀」是成因。
但同一文件里 `[前驱发作与真前驱是两件事，别写混]` 就点了阶段名
——**点名模型本来就看得见的状态是合法的**，不合法的是在档位行里写死成因。
所以照前驱那块的形状单开一块：

```
[孕早期那个胸胀跟泌乳不是一件事，别写混]
- 那一段胀痛最凶，但跟里面装了多少无关。乳意值这时候是 0，那是对的，不要拿值去写这个胀。
- 判据是排掉能不能解：这个排不掉，排也排不出什么，它得自己退。
- 方向相反：这个胀是一碰就疼、趴不下去、跑起来要护着；泌乳那个胀是一碰就漏。
```

**第一行要挡的正是「模型看见 0 就不写胀」**——那一段面板上是空的，不是有个小读数。

第二行那个判据是从 §六 第 1 条反推的（「排出去是立刻的、大幅的松」＝存出来的胀的定义）。
跟「渗还是喷，判据只有有没有酥麻前兆」是同一种写法：**一句话能分开的，不要给两套机制。**

**不给它值**，判准是：**它是那一段的常量**——不随排空变、不随档变、不随她做了什么变，
光看孕周就能推。插件该管的是会变的东西。

这条判准正好解释了一件看着不一致的事：**非孕的周期性胸胀反而是有值的**
（黄体期 0.15/时、月经期 0.10/时）。凭什么那个给值、这个不给？
因为周期那个会涨会清零，孕早期这个不会。**分界不是孕期／非孕期，是会不会变。**

将来真要给它数，**挂性欲那份的下凹曲线，别开新旋钮**
（`_性欲值PRD.md` §6.1 的起点 4~8 周／深度／回位三个），底下是同一个早孕反应，
两处读同一条曲线不会漂。世界书 uid 11 现在就在写这件事，正文侧本来就有出口。


### ⚠️ 「渗」和「喷」不是程度差，是两套机制

**判据只有一条：有没有 0.5~2 秒的酥麻前兆。有＝喷，没有＝渗。**

| | 渗 | 喷 |
|---|---|---|
| 动力 | 重力＋乳窦满溢，被动漏出 | 排乳反射：催产素→肌上皮收缩，主动推出 |
| 前兆 | **无**，通常事后低头才发现 | **有**，酥麻从乳晕后方往乳头扩散 |
| 时程 | 持续、慢速 | 瞬时、脉冲，2~10 秒一波 |
| 方向 | 顺着往下淌 | 有射程，多孔各朝一个方向 |
| 日常占比 | **绝大多数**——乳垫湿、衣服黄印，来源全是渗 | 只在被触发时 |

### ⚠️ 射程由压力决定，不由奶量决定（素材说这条最容易写反）

「喷」要三个条件同时到位：

1. **排乳反射正在发生**——单纯蓄满不会喷，只会渗。压力来自肌上皮收缩，不来自装得多。
2. **乳孔通畅**——多被角质塞着的话，就算有反射也只能从一两个孔慢慢冒。
3. **有没有外力**——手挤乳窦能加压。**自发喷通常只有几厘米，多半就在内衣里散掉了。**

⚠️ **产前喷不远，别把产后量级挪到孕期。** 隔一米喷到人身上那种画面属于产后涨奶期。

### 饱和传递

布吃满了**不再扩大湿印，而是往下一层传**（贴身→外衣→坐卧的东西）。
所以高档的写法不是「湿印越来越大」，是「现在湿到第几层」。
第 37 条说这个阶梯到松档以上就不够用了——那时的问题不是「湿到哪一层」
而是**「有没有东西接」**。

### 蓄满 vs 刚排空是两种状态，不是一个值的高低

| | 蓄满 | 刚排空 |
|---|---|---|
| 触感 | 鼓硬发胀，从内部往外顶，按下去有阻力 | 软、松 |
| 外观 | 更圆更挺、皮肤绷亮、静脉凸起；乳头更翘 | 回到平时 |
| 敏感度 | **极高**，衣服蹭到就有感觉，碰乳头就漏 | **降低** |
| 主观 | 想挤、想碰、想排掉 | 清爽，暂时不管它 |

**用法**：排空之后那一小段要写「刚排空」这一档，不是继续写胀。
反过来久没排的时候（早上刚醒、忙起来忘了）才是一碰就漏的状态。

### 夜里最满（进正文，不挂机制）

不只是攒得久——催乳素在睡眠中本来就是全天生理峰值。所以**晨起那一次量最大**。
「床单湿了，早上还能挤出一大管」这两件事同时成立，因为漏出和留存是两笔账。

**为什么只能进正文：插件没有时辰概念。** 全库唯一提到时辰的地方是
`prodromal_config.js` 那句「夜间尤甚：这个乘数留给引擎按时辰用，
**没有时辰概念时不生效**」——`nightMultiplier: 1.5` 是有意留着的死代码。
所以「夜里最满」不是我们不想做，是没有可读的时间。同理不要给它设计任何数值旋钮。

### 启动那一段的写法

第一次出奶**很少是一个能指认的时刻**，真实的形状是：
某天发现了一样已经在那儿好几天的东西。最早的可见证据是乳头孔上那块干掉的初乳痂
（黄褐、硬、发亮，像皮屑），比任何湿的痕迹都早。
另两种被发现的方式都是外力先破了那层痂：热水冲过之后，或睡醒发现贴身那层跟乳头粘在一起。

**这一段还没有排乳反射**（没有酥麻前兆，压了有不压没有），
**性质这时候最浓**（黄到发橙、稠得挂在那儿不往下滴、量极少）
——往后是越来越稀越来越多，**所以起头那一滴反而是整个孕程颜色最深的**。

### 产程期间（通用书 30 条，它自己说写成背景即可）

> ▸乳房：强烈用力加催产素峰值 → 胀感明显、渗得比平时凶，可能一阵阵打湿衣物。
> **这一层通常被下身盖过去，写成背景即可。**

所以**不做机制，只在正文口径里留一句**：产程中渗得比平时凶，但不抢戏。

---

## 十、待拍板

**方向类**
- [x] 默认档＝中（2026-08-30 拍板）
- [x] **档定死，不随行为上调**，只由用户改或剧情外力（2026-08-30 拍板）
- [x] 临时触发＝排掉一笔＋低谷，不加总量（素材硬口径）
- [x] **值保持抽象，毫升只进配置层**（2026-08-30 拍板，见 §三点七）
- [x] 产程期间的乳意：不做机制，正文留一句（30 条自己说写成背景即可）
- [x] ~~「憋」那条衰减做成什么形状~~ 见下面数值类那条。
- [x] **六个档各自什么感觉**（2026-08-31 第三轮，§三点六）：无／低／中／高／满／爆，
      用插件现成档名不另起数字；**无档和低档必须留**，否则她一年三百天都在胀。
- [x] **值量的是「撑」不是「有多少奶」**；「撑不撑得住形」是同一个值的外在表现，不用新字段。
- [x] **溢出只有两道线**：50 以上挤才出、100 以上碰一下就漏或自己往外走。
      「一直在漏」不是第三道线，是她一直停在 100 以上下不来。四档的四张脸全是这条线落出来的。
- [x] **进 guide 是十二行**，详细版留 PRD 不进插件；世界书 uid 37 已有的（初乳颜色、
      那块痂、湿到第几层、喷不远、夜里最满）一律不重复。见 §九点五。
- [x] **孕早期那个胀单开一块 guide、不给值**，判准是「它是那一段的常量，不会变」
      ——分界不是孕期／非孕期，是会不会变。见 §九点五。
      **面板读数＝0（2026-08-31 一并定死）**：周期那条路不给（她不在周期里了）、
      泌乳那条路也不给（还没到启动周）。§三点六 天花板表原写「低」，已改成「无」。
- [ ] 排奶要不要接进催产素→宫压那条链（松档以上会成为比高潮更高频的催产源）
      **接法已定：一次性加一笔，不做抬地板，见 §七。**
- [x] ~~排卵期那个 `delta × 0.05` 留还是删~~ **删**（2026-08-31 第三轮，第 37 条里无依据）
- [x] **产后整块不做，断奶不做**（2026-08-31 第四轮，用户拍板）：产后交给模型自行判断，
      代码现有行为即最终行为。见 §八。
- [x] **常规排掉的门＝「这段时间允不允许」，不是「够不够时间」**（2026-08-31 第四轮）：
      场景连续推进不排、剧情跳时间当她排过了。**插件不需要知道上课／在人前／睡着。**
      见 §四点五。
- [x] **加减值整套**（2026-08-31 第四轮，§四点五）：四档速率 3.4／20／67／250，
      残值＝封顶两成，事件三档 轻 3~8／中 8~20／强 20~35，性欲折两成，
      同档相加、超了按上一档算且封在那一档的顶（最多顶一档），
      一段连续刺激只推一次（不需要机制，模型一回合报一笔），
      漏不减值（封顶＝平衡点），排一次不一定排空（三档落点）。
      **数全部是我提的、用户看过形状，还没逐个拍板。**
- [ ] 临时触发要不要走单独的工具参数，还是复用 `options` 里现有的口
      （**跟性欲那边「三类刺激分钟怎么报」可能该共用一个入口**，两份 PRD 一起看）
- [x] ~~**周期型／泌乳型要不要拆成两个字段**~~ **不用拆**（2026-08-31 第三轮）：
      差别全在天花板（非孕压在低档、孕期抬上去），同一档给同一个感受；
      「靠阶段猜该不该清零」猜的就是天花板。见 §三点七 末。

**数值类（全部未定）**
- [x] ~~四档各自的满线／天花板~~ **不需要线表**：一套线（沿用 25/50/75/100/125/150），
      档只给**这一趟的天花板**。见 §三点六。
- [x] ~~各档启动周数怎么落到代码~~ 按天插值不挂阶段（见 §三点八 保留项第 3 条）。
- [x] ~~长期上升那条曲线的形状~~ 同上：四档共用一条形状，差别在起点周和天花板。
- [x] ~~「憋」那条衰减做成什么形状~~ **不做衰减**：见下面「奶不会攒」那条。
      （第一版给的 15%/小时向下回归，形状其实是「漏」不是「衰减」，
      而漏已经由 §三点六 那两道线管了。）
- [x] ~~自发溢出那个地板各档给多少~~ **不需要地板**：过 100 就漏这一条已经在做这件事，
      而「一直在漏」＝她一直停在 100 以上。见 §三点六。
- [x] ~~一次排乳反射排掉多少~~ **按剧情实际排了多少，三档落点**（掏空＝封顶两成／
      排了大半＝封顶一半／几滴＝减 5~15）。按比例不按固定数，见 §四点五。
      **顺带否掉 `bsExcreteMetabolism` 那个固定 −30。**
- [ ] **反射建立隔启动周多久**（素材只说「启动那一段还没有排乳反射」，没说多长）
- [x] ~~一趟爬多快各档给多少~~ **紧 3.4／中 20／松 67／全开 250 每小时**（足月），
      摆法见 §四点五。数未逐个拍板。
- [x] ~~一段连续刺激之后的不应期多长~~ **不做，整条删了**（2026-08-31 第四轮，用户提的）：
      顶飞这件事封顶本来就管住了，不用第二道。「揉两分钟和揉半小时一样」是白拿的
      ——模型一回合报一笔。见 §四点五。
- [ ] **性欲折过来那个比例**：提议两成；代码现在 `0.18` 量级对，
      但后面多乘了个孕周系数（足月 1.9 倍＝放大成三成半），那一处要拆掉。
- [x] ~~「排了会更多」那个正反馈的系数与上限~~ **整条不做**（2026-08-31 第三轮，用户提的）：
      跟「憋着产得少」一起删——两条是同一机制的两个方向，删一半就剩个单向系数在漂。
      理由：「行为推产量」跟已拍板的「档定死、模型不能动」对台。
      **产量只看孕周和档位**，少两个系数。见 §六。
- [x] ~~反锁的门槛~~ **时长，不是值**：满档以上没排乳，约 4／8／16／24 小时四段（§三点九 表三）。
      具体时长和恢复条件（热敷／时间）未定。
- [x] **「奶不会攒」改由常规排掉兑现，不用衰减**（2026-08-31 第三轮）：
      照尿意 `applyRoutineUrineVoid`（tools.js:2329），她能排的时候自己排了、不出声。
      **门的判据第四轮定死了：不是「够不够时间」，是「这段时间允不允许」**
      ——场景连续推进不排、剧情跳时间当排过了，**插件不用知道上课／在人前／睡着**（§四点五）。
      于是「夜里最满」不用挂机制、**反锁降回剧情事件**。
      计数器只剩反锁这一件活，不再压产量；**而且只有真排掉才清零，漏不清零。**
- [ ] 喷那张概率表各档的数，以及四档各自的乘数（形状照 `URINE_STRESS_LEAK_CHANCE`，见 §三点九 表二）
- [x] ~~产后乘二到三倍的具体数、质变那一层怎么表示~~ **产后整块不做**，见 §八。
- [x] ~~「她湿多少」那张表~~ **不用另做**：平衡时往外走的速度就等于那一档每小时爬多少
      （紧 0／中 0／松 67／全开 250），见 §四点五「漏不减值」。
- [x] ~~加权项各给多少~~ 方向已定：**只动启动周 ±1~2 周**
      （否则几项一叠会摸到全开，而全开只能靠外力）。具体几周未定。

**语义类**
- [ ] 「乳意被阻塞」（`blockage.milk`＝乳房胀痛敏感）跟「憋」是不是同一件事
- [ ] `acceleration.milk`（乳意快升、溢乳）跟档位是什么关系——会不会两个旋钮打架
- [ ] `immune.metabolism` 是否连带冻结泌乳（性欲那份也有同一条）
- [x] ~~**面板显示**：四档的线差五十倍~~ **消掉了**：四档共用一条线，百分比直接可比。
- [ ] 爆档权重 `base: 0`（身体不再自己攒）跟全开档「无时无刻都在流」冲突
      ——乳意大概也要自己一张分档权重表，跟性欲那份是同一个坑。

**代码对不上的五处（不是待拍板，是要改的）**
- [ ] `getMilkFetalLoad` 里 `0.15 + progress`（[tools.js:1915](scripts/tools.js#L1915)）
      **从受孕第一天就在加**，跟启动周（紧 22／中 16／松 13／全开 11）冲突。
      **顺带把孕早期那段夹平到 0**（§三点六 天花板表已定 0）。
- [ ] 紧档「全孕程不用任何垫吸物」跟高档定义里的决策层（要垫、算多久换）差一点
      ——正文口径要补一句：**够不到湿，那垫是防万一不是防漏。**
- [ ] `applyMilkFromLibido`（[tools.js:2150](scripts/tools.js#L2150)）要改**四处**，
      **但方向是对的**（性欲涨→乳意涨）：只认刺激不认基线漂移、没有反射的那一段不加、
      删掉排卵期那个 `delta × 0.05`、**把后面那个孕周系数拆掉**（足月把 0.18 放大到 0.35）。见 §五、§四点五。
- [ ] **`applyCycleBreastNeedGain` 里月经期 `+0.10/时` 符号是反的**
      （[tools.js:2135](scripts/tools.js#L2135)）：经前最胀、**月经一来就缓解**，
      所以这一段该往下走。量级上要能把黄体期攒的那 43 抹掉
      （5 天 × 24 × 0.4 ≈ 48），具体数未拍板。
      §三点六 那张周期表里「0.10/时，来了之后开始退」是照现有代码写的，改完要一起改。
- [ ] **`tracker_prompt_context.js:152` 那句「milk…排卵期可因性欲波动少量累积」要一起删**
      ——它是 `delta × 0.05` 的使用说明书。只删函数不删这句，模型会自己编排卵期的读数。
- [x] ~~产后那两处（`getMilkFetalLoad` 固定 1.35、`canProduceMilk` 没有断奶）~~
      **不改了**：产后整块不做，现有行为即最终行为。见 §八。
- [x] ~~`bsExcreteMetabolism` 那个固定 −30 不跟档~~ **换成按封顶比例落**，见 §四点五 减那一节。

---

## 十一、两份 PRD 共用的东西（改一处要记得改另一处）

现在在两份里各写了一遍，以后容易漂：

| 共用项 | 在性欲那份 | 在这份 |
|---|---|---|
| 宫颈成熟度是第二条独立的轴 | §四 | §七 |
| 催产素那条链（→宫压／→溢乳） | §四 唤起→宫缩 | §七 |
| 「抬地板」这个形状 | §四（性欲压不住宫缩） | §三点五 自发溢出 |
| 按天插值、不挂阶段 | §6.1 | §三点八 保留项第 3 条 |
| 引素材的判准（因果可搬、周数和 mL 不搬） | §八 | §三点五末 |
| 临时事件走哪个口报 | §七 | §十 |
| **要自己一张分档权重表**（`base: 0` 在爆档不适用） | §二 分档权重 | §三点八 末那个坑 |
| **几笔一起来：加起来，但最多顶一档** | §二 末（**要补「最多顶一档」**） | §四点五 |

**⚠️ 有两条曾经想对齐、现在确定两边故意不一样，别去统一：**

1. **「向基线回归、超出部分按比例衰减」只在性欲那边有。**
   性欲有基线、值要落回去；**乳意没有衰减这回事**——它靠排掉掉下来（§四点五、§六）。
2. **连续刺激靠什么停住**：性欲靠衰减（按分钟加也会越走越平，「顶着但到不了」）；
   **乳意靠封顶**（到本趟天花板就停，事件也推不上去，§四点五）。
   给乳意加衰减会把「奶不会攒」和「憋着」一起弄死；给性欲加封顶会把「顶着到不了」弄死。

