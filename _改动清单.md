# 性欲值 + 乳意 · 代码改动清单

状态：**乳意十处、性欲九处全部改完。测试 359 全过（新增性欲 41 条）。**
日期：2026-09-01

---

## 二〇二六〇九〇一 这一轮：性欲收尾 + 三个 bug

### 性欲剩下的四处

| | 改了什么 | 落在哪 |
|---|---|---|
| A | 高潮诱发排卵改成**挂在真高潮上、按档掷概率**（小 0／中 8%／大 35%） | `libido_config.js` `tools.js` |
| B | 对外视图 `buildLibidoView`：档名／百分比／附加标签／阶段说法 | `libido_config.js` `tools.js` |
| C | tracker 把性欲发给模型（发视图不发数字），提示词补 `[metabolism.libido]` 一整节 | `tracker.js` `tracker_prompt_context.js` |
| D | 面板：性欲那一行挂档名与标签 | `index.js` `style.css` |

⚠️ D 原来还包含「压抑芯片的 ON/OFF 开关」。那个开关**做出来之后又整个删掉了**
（见 §四点六）——它让用户手动决定档位，而档位该由剧情决定。

高潮排卵那一处的旧判据（值顶到天花板→额外排卵→性欲归零）两条都跟口径对台，
已经废掉：现在去不去由闸决定，值落到哪由 `fireLibidoOrgasm` 按档处理。
`bio.orgasmOvulationAmount` 仍管排几颗，为 0 的种族（精灵）连骰都不掷。

### 顺手修掉的三个 bug

1. **面板性欲恒读 0。** 搬家时 `state.js` 把 `base.libido` 删了，
   但 `index.js:3034` 还在读它。同时 index.js 自己那份 `getLibidoCap`
   是按月取整的旧公式（100→150），跟三条线完全不是一回事（入盆后天花板是 72）。
2. **面板乳意的「满了几成」分母写死 150。** 引擎侧容量已经随孕周从 18 长到 150，
   两边不一致：同一个存量 30，20 周该读「满」，面板读「低」。
   容量与发育进度提到 `metabolism_config`（`getMilkCapacityFromDays`），两侧读同一份。
3. **走路会攒电荷。** 电荷最低那一档门槛在 `peak: 0`，而环境类的闸在值没过满线时
   一步都不爬 → 走一整天被记成二十四次「刚起来就停」＝ 96 电荷，
   下一次真去直接读**大档**。而口径 §五 说大档要边缘四次（4×18＝72）。
   门槛改成 `peak: 1`：「刚起来就停」得先起来过。

### 还改了一处口径落地不对的

**性欲的分档原来按天花板等比缩放**（`getMetabolismLevel`），
入盆那一段会把满档门槛压到 `100×72/150 = 48`，而满线是 58
——于是她**坐在起点 50 什么都不做就读成「满」**，而满档的基础侧权重是 0。
口径 §二 说「跟尿意同一套切法」、§八 说「起点 50 落在高档」，
所以改成按满线切（`getLibidoLevel`／`getLibidoBandWeights`）。
其余三段两种切法结果一致，因为那三段满线／天花板的比例都在 2/3 附近，
恰好撞上 100/150——**那是巧合，不是对齐。**

**衰减原来在想象／环境类进行中照跑。** `idleMin` 读的是 `landed`（只认 C3/C4/C5），
于是 C2 每分钟同时吃进增量和衰减：高档时增量 3.2/小时、衰减 6/小时，净负。
结果口径两句话都落不了地——「走一整天可以到满」永远走不到，
入盆后（起点就在高档）增量干脆是 0。判据改成「这一分钟有没有刺激」，
不是「有没有身体接触」；口径那句「只有停手」讲的是刺激停了，不是手离开了。
改完：孕中／孕晚／入盆走十六小时都能到满线，非孕期到不了（孕期那 2.2 倍才是来源）。

### 新增测试 41 条

- `tests/libido.test.mjs`（22 条）：配置层——三条线次序、早孕下凹、入盆断点、
  按天连续不出台阶、分档按满线切、电荷按次攒、三条路的档位、视图不含隐藏量。
- `tests/libido_engine.test.mjs`：端到端走 `applyToolCall`——
  落回起点不落回 0、接触中不许往下走、闸只在过满线后爬、想象永远推不动闸、
  一直死顶永远是小、虚报分钟被掐掉、性欲排不掉。
  （原来有一条「压抑按住闸但电荷照攒」；机制删掉之后换成了三条**防它被加回来**的：
  老存档残留不改变行为、引擎不再写那个键、同样的做法必须得到同样的档位。）

---

以下是上一轮（乳意）的记录。

## 已经改完的（乳意）

| | 改了什么 | 落在哪 |
|---|---|---|
| 1 | 四档闸表 `MILK_GATES`（开奶周／产量／漏掉／单次挤出／溢出阈值／会不会喷） | `metabolism_config.js` |
| 2 | `getMilkCapacity` + `getMetabolismCap` 加乳意分支 | `tools.js` |
| 3 | 两处写死的 150 换成 `getMetabolismCap` | `tools.js` |
| 4 | `getMilkFetalLoad` 去掉常数 0.15、接上开奶周 | `tools.js` |
| 5 | 月经期 +0.10 改成 −0.6（符号反了） | `tools.js` |
| 6 | 删排卵期那个 `delta × 0.05`，连带删 tracker 里那句说明 | `tools.js` / `tracker_prompt_context.js` |
| 7 | `applyMilkFromLibido` 不再走 `applyMilkGain`（拆掉重复的孕周放大） | `tools.js` |
| 8 | 排掉的固定 30 改成按档「单次挤出」 | `tools.js` |
| 9 | 出口状态（通着／结痂）＋ 堵的计数器，新字段 `profile.milk` | `tools.js` |
| 10 | 冻结时乳意不再抹成 0 | `tools.js` |

新增：`bio.milkGate`（建卡时选档）、`tests/milk_gate.test.mjs`（10 条）。

**动手时定下来的两个数**（原来是空的，见 §四）：
- 容量下限 `MILK_CAPACITY_MIN = 18`。不能给 0——`getMetabolismLevel` 里
  `Math.max(1, cap)` 会把极小容量放大成「存 1 点就爆档」。
- 结痂时长 `MILK_CRUST_HOURS = 6`。

**两处跟口径对不上、按代码来的**：
- **满度分档的边界。** 插件原生是 16.7/33.3/50/66.7/83.3%，
  口径 §五 写的是 15/35/55/75/90%。没去覆写分档函数——它是七个代谢键共用的，
  为乳意单开一套会分叉。差值最大一档 7 个百分点，档名承载的体感没变。
- **产后那条路没动。** `产后恢复` 和 `假孕期` 仍走原来的 `0.08 × 孕周系数`，
  只有真妊娠走档位表。口径 §十三 写的是「产后整块不做」。

---

以下是原始核对清单。

设计出处：
- `_性欲值_现行口径.md`
- `_乳意_现行口径.md`

两份 PRD（`_性欲值PRD.md`、`_乳意PRD.md`）是过程记录，**冲突时以现行口径为准**。

---

## 零、一个好消息：分档不用改

`getMetabolismLevel`（[metabolism_config.js:153](scripts/metabolism_config.js#L153)）
本来就是**按上限缩放**的：

```js
const scale = Math.max(1, Number(cap) || BASE_METABOLISM_CAP) / BASE_METABOLISM_CAP;
if (value >= 125 * scale) return '爆';
...
```

所以上限传 60 的时候，六档自动落在 10／20／30／40／50。

**乳意那份 §五「她多难受看满了几成」插件原生就支持**，
只要 `getMetabolismCap` 对乳意返回「当前容量」而不是写死的 150。
面板显示的百分比也跟着自动对。**这一处一分钱不用花。**

---

## 一、乳意：要改的十处

### 1. 上限要随孕周走（新增，这是地基）

**现在**：`getMetabolismCap`（[tools.js:1856](scripts/tools.js#L1856)）只给尿意开了特例，
其余全部返回 `BASE_METABOLISM_CAP`（150）。

**要改成**：加一个乳意分支，返回该档当前的容量。

```js
const baseCap = key === 'urine'
  ? getUrineHardCap(...)
  : key === 'milk'
    ? getMilkCapacity(profile)     // 新增
    : BASE_METABOLISM_CAP;
```

`getMilkCapacity` 是新函数：开奶周之前返回 0，之后按天插值涨到该档足月值。
**不挂阶段**（挂阶段会出台阶）。

**为什么这是地基**：不改这一处，「满了几成」就等于「绝对值除以 150」，
于是早期永远读不到高档，§五 整节落不了地。

### 2. 两处写死的 150 要换掉

- `applyMilkGain`（[tools.js:2130](scripts/tools.js#L2130)）：`addMetabolismValue(profile, 'milk', ..., 0, 150)`
- `applyCycleBreastNeedGain`（[tools.js:2137](scripts/tools.js#L2137)）：同上

**都要换成 `getMetabolismCap(profile, 'milk')`。**

顺带一处不一致：`state.js:934` 和 `tools.js:458` 允许乳意到 **200**，
可这两处加值写死 150。**统一走 `getMetabolismCap`**（它自己会处理扩容那一档）。

### 3. `getMilkFetalLoad` 从受孕第一天就在加

**现在**（[tools.js:1904](scripts/tools.js#L1904)）：

```js
const progress = clampNumber(effectiveDays / 280, 0, 1.5, 0);
return clampNumber((0.15 + progress) * (...), 0, 12, 0);
```

`0.15 + progress` 里那个 **0.15 是常数**，所以受孕第一天就有产量。

**要改成**：开奶周之前返回 0，之后从 0 开始按天涨。
开奶周按档给：紧 22／中 16／松 13／全开 11 周。

**顺带把孕早期夹平到 0**（面板读 0，那一段的胀走正文）。

### 4. 月经期符号是反的

**现在**（[tools.js:2135](scripts/tools.js#L2135)）：

```js
const hourlyRate = stage === '黄体期' ? 0.15 : stage === '月经期' ? 0.10 : 0;
```

月经期 **+0.10 是反的**。经前最胀、**月经一来就缓解**，该往下走。

**要改成 −0.6/时**：黄体期攒下 0.15 × 24 × 12 ≈ 43，
要在三天内清掉就是 0.6。（0.36 是拖满五天，素材口径是「一来就缓解」，取 0.6。）

排卵期是 0，**现在的代码这里就是对的，别动**
——推动乳房胀的是黄体素，它只在排卵之后才升，所以那两天是整个周期的谷底。

### 5. `applyMilkFromLibido` 三处

**现在**（[tools.js:2150](scripts/tools.js#L2150)）：

```js
if (String(profile?.base?.stage || '') === '排卵期') {
  addMetabolismValue(profile, 'milk', delta * 0.05, 0, 150);   // ← 删
}
applyMilkGain(profile, delta * 0.18);
```

**方向是对的（性欲涨→乳意涨），别反过来。** 要改三处：

1. **删掉排卵期那个 `delta × 0.05`** ——世界书 uid 37 里找不到依据，插件自己加的。
2. **只认外部刺激那一笔**，不认基线漂移。新口径下性欲每分钟都在动
   （基线、衰减、习惯化），照现在这个写法她躺着不动也会产奶。
3. **没有排乳反射的那一段不加** ——开奶那一段是纯机械挤压，
   没有「往前推」这件事，所以刺激不该让值跳。

另外 `applyMilkGain` 里又乘了一次 `getMilkGainMultiplier`（孕周系数，足月能到 1.9），
**等于把两成放大成三成半**。折两成就是折两成，这一层要拆掉。

### 6. `tracker_prompt_context.js` 那句要跟着删

[tracker_prompt_context.js:152](scripts/tracker_prompt_context.js#L152) 里写着
「milk…排卵期可因性欲波动少量累积」。

**这句是上面那个 `delta × 0.05` 的说明书。**
只删函数不删这句，模型会自己编出排卵期读数。**两处必须一起删。**

### 7. 排掉那个固定减 30

**现在**（[tools.js:2889](scripts/tools.js#L2889)）：

```js
const milkReduction = ... optionReduction('milk', useDefaults ? 30 : 0);
```

固定 30 对四个档完全不是一回事：紧档单次挤出 2~5mL，全开 150~250mL。

**要改成按该档「单次挤出」算**，或者按容量的百分比。

### 8. 出口状态是新的，插件里没有

乳意 §六 那块痂需要一个新状态：**通着 / 结痂**。

- 结痂 → 自发溢出掐成 0，**但存量照涨**
- 抠掉 / 有往外走或喷出去 → 恢复通着
- 长期只有干在原地或渗一点点 → 结痂

**现在插件里一点相关的东西都没有。** 要新增一个字段（`milk.duct` 之类）。

### 9. 堵的计数器也是新的

乳意 §七 那四段（4／8／16／24 小时）要一个计数器：
**满度七成以上且没有效排空**的时候走，排掉清零。

最后那一格（堵住）**排不掉**，要单独的恢复条件，所以它得是个独立字段
（`milk.blockHours` 之类），不能只是个读数。

**注意跟现有的 `blockage.milk` 分清**：那个是「妊娠阻塞状态」，
让 `bsExcreteMetabolism` 排解不顺畅（[tracker_prompt_context.js:168](scripts/tracker_prompt_context.js#L168)
写的是「乳房胀痛、敏感」）。**是不是同一件事，待拍板**（见现行口径 §十四）。

### 10. 冻结的时候是清零，不是冻住

**现在**（[tools.js:2619](scripts/tools.js#L2619)）：`immune.metabolism` 为真时
`metabolism.milk = 0`，直接抹掉。

按现行口径，冻结该是**停止变化**而不是归零。
四个量（存量、容量、出口状态、堵的计数器）各自怎么处理，**待拍板**。

---

## 二、性欲：要改的九处

**这一份比乳意动得大** ——性欲现在住在 `base` 里，要整个搬进 `metabolism`。

### 1. 搬家：`base.libido` → `metabolism.libido`

**现在**：性欲在 `base.libido`（[state.js:820](scripts/state.js#L820)，0~150）。

**要搬进 `profile.metabolism`**，理由是 `addMetabolismValue` 读写的是
`profile.metabolism[key]`，要用它那套「这一份算哪一侧」的分档权重就必须是代谢键。

**加进 `METABOLISM_KEYS` 一处**（[metabolism_config.js:12](scripts/metabolism_config.js#L12)），
序列化／每 tick 夹边界／补丁键集／标签全部自动跟上。

要动的地方：
- `METABOLISM_KEYS` 加 `'libido'`，`METABOLISM_LABELS` 加 `libido: '性欲'`
- `base` 字段清单里摘掉 `libido`（[state.js:802](scripts/state.js#L802)）
- `METABOLISM_FIELDS`（[registry.js:1011](scripts/registry.js#L1011)）加进去
- 注册时的清洗（[registry.js:1277](scripts/registry.js#L1277)）改路径

### 2. `getLibidoCap` 是删掉，不是修

**现在**（[tools.js:2190](scripts/tools.js#L2190)）：

```js
function getLibidoCap(profile) {
  if (!isTruePregnancyStage(stage)) return 100;
  const months = Math.floor(effectivePregnantDays / 28);
  ...
  return Math.round(100 + (150 - 100) * progress);
}
```

**新口径下这个函数整个不需要了。** 新的是三条线（起点／满线／天花板）随孕期走，
按天插值不按月取整（`Math.floor(days / 28)` 会出台阶）。

**两个调用点**：
- [tools.js:3638-3639](scripts/tools.js#L3638) 产程那个 `libidoMultiplier`
- [tools.js:4450](scripts/tools.js#L4450) `maybeTriggerOrgasmOvulation`

### 3. 产程那个乘数要改成读「高出起点多少」

**现在**（[tools.js:3639](scripts/tools.js#L3639)）：

```js
const libidoMultiplier = 1 + (libido / Math.max(getLibidoCap(profile), 1)) * 0.25;
```

**方向是对的**（高性欲推产程），跟素材一致。**别删，只改读法。**

问题是它读的是「值除以上限」这个比值。孕晚期起点抬到 42~50，
**于是她什么都不做也一直是高乘数。** 要改成读**高出起点多少**。

### 4. `maybeTriggerOrgasmOvulation` 跟新机制冲突

**现在**（[tools.js:4441](scripts/tools.js#L4441) 起）：值顶到上限就触发排卵、
然后 `base.libido = 0`。

**跟新口径冲突两处**：
- 「值顶到头等于高潮」是明确否决掉的（现行口径 §十三）
- 归零跟「有基线、落回基线不落回 0」冲突

**⚠️ 这一处用户说过：要拆，但实际执行前要先问。** 别自己动。

### 5. 分档权重要自己一张表

**现在**（[metabolism_config.js:168](scripts/metabolism_config.js#L168)）只有一张表，
是为尿意调的：低档 `stimulus: 0.5`（憋着时外部刺激更容易挤出来）。

**性欲要反过来** ——低位想象有效、高位身体接触有效。所以要另一张：

| 档 | base | stimulus | decayPerHour |
|---|---|---|---|
| 无 | 0.8 | 1.15 | 0 |
| 低 | 0.6 | 1.25 | 2 |
| 中 | 0.5 | 1.20 | 4 |
| 高 | 0.25 | 1.40 | 6 |
| 满 | 0 | 1.60 | 8 |
| 爆 | 0 | 1.80 | 3 |

**实现**：`METABOLISM_BAND_WEIGHTS` 从一张表改成按 key 查
（`{ urine: {...}, libido: {...} }`），查不到走中性值（base 1／stimulus 1／decay 0）。

**⚠️ 不能写成 `key === 'urine' || key === 'libido'`** ——两者方向相反。
其余五项行为完全不变。

### 6. 三个隐藏量是新的

| 量 | 干什么 |
|---|---|
| **电荷** | 决定去了是小中大。**按次攒，不按分钟** |
| **闸** | 爬满了就是去了。爬多快看值超出满线多少 |
| **习惯化** | 同一档一直做，闸爬得越来越钝 |

外加**不应剩余**和**这一次来自小/中/大**。

**插件里一个都没有，全是新字段。**

### 7. ~~压抑芯片要一个用户能拨的开关~~ 已作废，机制整个删掉

**这一条做完之后被推翻了，见 §四点六。**

原计划：面板上一个用户手动拨的 ON/OFF，开了之后闸速打二五折。
推翻的理由是**它让用户手动决定小中大，而档位该由剧情里发生了什么决定**；
而且它跟自己的说明书方向相反（按住闸 → 峰值更低 → 攒得更少 → 去了还更小）。

「憋着憋着一放开特别凶」这个效果改走 §五点五 那条路
（反复被撩起来又丢下会累积），那条路是剧情自然落出来的，不用谁去拨开关。

### 8. 必须读分钟，不能读小时

**`buildTimeTick`（[tools.js:4485](scripts/tools.js#L4485)）里
`passedHours: Math.floor(totalHourMinutes / 60)` 是整数小时。**

性爱场景一轮推 5~15 分钟，**多数轮 `passedHours = 0`**，偶尔跳 1。
按小时算的表现不是滑落，是「连着几轮不动，然后一次掉一大块」。

`deltaMinutes` 在同一个 tick 对象里、不量化，**改读它是纯新增，不动别的**。

小时那一层只留给跨场用（电荷的自然消退、值朝起点落）。

### 9. 刺激分钟要单独报，还要掐住

模型每回合报「哪一档、几分钟」。**刺激分钟 ≠ 本轮分钟**
——一轮可能覆盖半小时但只有五分钟在动手。

所以要新增一个报点，**并且拿本轮实际时长掐住，报多了截掉**。

---

## 三、动手顺序（建议）

**乳意先做，性欲后做。** 理由：乳意只改现有函数、加两个字段；
性欲要搬家、要改共用的权重表，动的面更大。

### 第一批：乳意的地基（这一批做完就能看出效果）

1. 写 `getMilkCapacity`（开奶周之前 0，之后按天插值）
2. `getMetabolismCap` 加乳意分支
3. 两处写死的 150 换成 `getMetabolismCap`
4. `getMilkFetalLoad` 去掉那个常数 0.15、接上开奶周

**做完这一批**：「满了几成」就对了，早期不会再一攒久就进高档。

### 第二批：乳意的小修

5. 月经期符号（+0.10 → −0.6）
6. 删排卵期那个 `delta × 0.05`，**连带删 `tracker_prompt_context.js:152` 那句**
7. `applyMilkFromLibido` 另两处（只认刺激、没反射不加）＋ 拆掉那层孕周放大
8. 排掉那个固定 30 改成按档

### 第三批：乳意的新东西

9. 出口状态（通着／结痂）
10. 堵的计数器
11. 冻结时怎么处理

### 第四批起：性欲

先搬家（1），再删 `getLibidoCap`（2、3），再分档权重表（5），
最后三个隐藏量和芯片（6、7）。**`maybeTriggerOrgasmOvulation`（4）动手前问用户。**

---

## 四、动手前必须先定的数

**乳意**（不定这些，第一批就写不出来）：

- **容量那条线**：开奶周是 0，足月是多少，中间怎么走
- **产量那条线**：同上
- 四档的自发溢出阈值
- 溢出速度各档多少（现行口径 §三 那张表里「漏掉」一列是我倒推的，没拍板）
- 结痂要多久
- 排乳反射隔开奶周多久建立
- 堵那四段时长（4／8／16／24 是提议）和恢复条件
- 跳时间排掉之后落到多低

**性欲**：

- **电荷那三档各攒多少**（现行口径 §五 只有「少／中等／多」）
  ——**这是唯一一处影响结构能不能跑的空缺**
- 习惯化的量级（提议 +0.022/分、上限 0.50、换档 −0.12/分、去了一次乘 0.35）
  **已知一个洞**：换档 4 分钟就从满值掉回 0。但它只影响多久去、不影响档位，不致命
- ~~压抑芯片的强度（提议闸速打二五折）~~ **已作废：整个机制删掉了（§四点六）**
- 三条线那张表里的数（七行都是参考标定，结构别动）
- 不应期在孕期缩短多少

---

## 四点五、补：他停手之后那几分钟（把她当物件的那个错）

**用户指出的问题**：原来「他停手」跟「她躺着睡觉」在系统里走同一条路——
读数往起点滑、闸对折。于是一个刚被撩到快要了的人和一个睡着的人，
这十五分钟一模一样。用户原话：「相当于他撩她的时候她会涨、他停下来的时候
就跟时间静止了一样，这样是不对的。」

按实际情况补了四条（口径 §五点五）：

| 改了什么 | 常数在哪 |
|---|---|
| 余韵：停手后那根条停在原地不掉，闸不滑、贴边还会往上爬 | `LIBIDO_AFTERGLOW_BY_PEAK` |
| 「他没收住」自己长出来，不写死不可逆点 | `LIBIDO_AFTERGLOW_GATE_CURVE` |
| 憋劲：反复被撩起来又丢下会累积，起得更快、退得更慢 | `LIBIDO_DENIAL` |
| 习惯化在停手期间也退 | `LIBIDO_HABIT_IDLE_DROP_PER_MIN` |

**新增隐藏量**：`afterglowMin`／`afterglowPeak`／`denial`，一律不进载荷
（`tests/libido_payload.test.mjs` 锁住）。余韵**只以标签形式**露出去（`tag: '余韵'`），
剩几分钟不给——给了等于告诉模型「还差多少」。

### 这一批里改掉的三处旧口径

**① 环境类不再算「一次」，所以不攒电荷、不留余韵。**
原来 §十三 写「C2 放大的是感觉和电荷」。那句跟标定自相矛盾：一小时攒 4，
走一天 96，下一次真去直接读大档——而 §五 说大档要边缘四次。
根子在「一次」的语义：有人在对她做一件事，有开头有结尾，停下来是个事件；
坐着顶着、走路磨着是底噪，没有「他停手」那一下。孕期放大的那一半没丢，
走的是另一条路（一天下来她坐在满线上，晚上那一次从高得多的地方起步，
峰更高，每一次攒得更多）。

**② 「入盆后满着也去不了」讲的是底噪，不是「有人弄她也弄不出来」。**
我在 `_模拟.mjs` 里把这条写成「入盆后轻档做 6 小时也不去」，那是我读过头了
——入盆不会让她丧失高潮能力，那个说法本身荒唐。
实测：入盆后环境类走一整天闸只到 10.7（永远送不过去，口径成立）；
轻档半小时一轮，一个半小时去一次（这是对的）。回归检查已拆成两条分开验。

**③ 余韵不许往那根条上加值**（两种加法都试过、都荒唐，详见口径 §五点五）。

### 顺带修掉的两个数值问题

- 「攒到极限却永远过不去」那个死局：一半是习惯化只增不减造成的
- 停手一小时之后比停手那一刻还高（爆档权重 1.8 叠上爆档只掉 3/小时）

`node _模拟.mjs 10` 直接看这一批的表现。

---

## 四点六、压抑芯片整个删掉（用户拍的）

**用户原话：「你如果真的搞成他控制不控制的住，用按钮来控制，那实在是太离谱了，
我是绝对不会答应的」「小中大高潮不应该是由我去主动控制的……这东西就应该剧情去控制」。**

删掉的两层理由：

**一、它跟自己的说明书方向相反。** 说明书写「闸被按住不让爬，电荷照攒，攒得越久
越厚，关掉那一下出来特别大」。但电荷读的是「这一次闸爬到多高才断」——按住闸就是
峰值更低，断掉时攒得**更少**。实测同一段剧本跑两遍，开了压抑那一遍电荷反而低。
实际效果是「更难去，而且去了还更小」，纯惩罚。

**二、更要紧的是它不该存在。** 一个按钮控制档位，就是 §十三 那条禁令
（「不忍只能小，忍了才能中大」）的另一种写法。

删掉的清单（每一条都写清楚那东西原来干什么用）：

| 位置 | 那是什么 |
|---|---|
| `libido_config.js` `LIBIDO_SUPPRESSION_GATE_SCALE` | 开了之后闸速乘 0.25 |
| `tools.js` `getLibidoGateSpeed` 里的 `suppressScale` | 把上面那个系数乘进闸速 |
| `tools.js` `getLibidoState` 里的 `suppress` | 从存档读那个开关的状态 |
| `tools.js` 四处 `suppress: state.suppress` | 写回状态时把它带上 |
| `index.js` `toggleSelectedTrackSuppress` | 点一下翻转开关、存档、弹提示 |
| `index.js` 「压抑」按钮 + 点击绑定 + `libidoSuppress` | 调试区那个 ON/OFF |
| 口径 §六 整节、§二 那一行、§十四 待定项 | 说明书 |
| `_模拟.mjs` 场景 4 与 `对照压抑()` | 原来专门用来查这个机制的那一节 |

**没有丢的东西**：「憋着憋着一放开特别凶」改走 §五点五（反复被撩起来又丢下会
累积）。「她忍着」不需要机制——旁边有人所以她不敢出声，那是正文写的事。

**留了三条测试防它被加回来**：老存档残留的 `suppress` 不改变任何行为、
引擎不再往状态里写那个键、同样的做法必须得到同样的档位。

⚠️ 老存档里那个键可能还躺着，**没人读它了**。不要因为看见残留就把机制加回来。

`node _模拟.mjs 4` 现在改成验「档位只由剧情决定」：一直做到底 → 小；
她自己弄两分钟停一下 → 大（这一行正是用户当初担心的那个情形）。

---

## 五、两处待拍板的语义（两份都有）

**`immune.metabolism` 冻结的时候，那些隐藏量怎么办。**
现在的代码是直接清零（[tools.js:2610](scripts/tools.js#L2610) 那一段）。

**`acceleration.milk`（乳意快升、溢乳，[tracker_prompt_context.js:176](scripts/tracker_prompt_context.js#L176)）
跟四档闸什么关系** ——会不会两个旋钮打架。

---

## 六、无法本地验证

用户说过：**插件在本地、不能实时调试，要等全部改完才会去试。**

所以每一批都要能单独跑测试。这台机器上跑测试的命令是：

```
node --test "tests/*.test.mjs"
```

**不能写 `node --test tests/`**，那样一定报错。

